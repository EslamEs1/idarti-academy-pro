---
description: "Task list for feature 003-courses-sessions"
---

# Tasks: Courses and Live Sessions Frontend

**Input**: Design documents from `/specs/003-courses-sessions/`
**Prerequisites**: `plan.md` (required), `spec.md` (required), `research.md`, `data-model.md`, `contracts/`, `quickstart.md` (all present)

**Tests**: Not requested in the feature specification. Validation is manual visual review against the 14 success criteria (SC-001…SC-014) plus per-page acceptance scenarios. Audit tasks in Polish are visual checks, not unit tests.

**Organization**: Tasks are grouped by user story per `spec.md` priorities (US1 P1 → US2 P1 → US3 P1 → US4 P2 → US5 P2 → US6 P3) after a small Setup + Foundational sanity check. Within each user story, tasks edit a single HTML file sequentially — `[P]` markers are used only in the Polish phase where audit tasks read different concerns.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Maps to user stories from `spec.md` (US1, US2, US3, US4, US5, US6); omitted on Setup / Foundational / Polish
- All paths are relative to repo root: `/media/mekky/work/backend/idarti-academy-pro/`

## Path Conventions

This feature continues Spec 001/002's static-site layout. There is no `src/` or `tests/` tree. All deliverables are six new static HTML files under `pages/student/`, plus a one-time CSS rebuild.

---

## Phase 1: Setup

**Purpose**: Confirm Spec 001 + Spec 002 baselines are still in place. No new dependencies, no new tooling.

- [x] T001 Verify Spec 001 + Spec 002 baselines: list `assets/css/output.css` (must exist and be ≤ 41 KB at start, will rebuild later); list `assets/js/main.js` (must contain only the four sanctioned handlers: drawer / dropdown / modal / tabs — and the existing `data-tabs` ARIA-tablist handler at lines 47-66); list `components/student-sidebar.html` (15 entries) and `components/header.html` (bell as `<a href="notifications.html">`); list `pages/student/dashboard.html`, `learning-journey.html`, `weekly-plan.html`, `notifications.html` (Spec 002 outputs intact). If any baseline file is missing or shows drift, halt and instruct the user to complete Spec 002 first.

---

## Phase 2: Foundational (Shell Sanity Check — Zero Deltas)

**Purpose**: Confirm there are NO shell deltas required for this feature (Spec 002 already added the two new sidebar entries and converted the bell). This is a documentation-only sanity gate so the implementer doesn't accidentally edit shell partials.

**⚠️ CRITICAL**: T002 must complete before any user-story page work begins.

- [x] T002 Confirm zero shell deltas per `plan.md §Project Structure` and `research.md §R2`: NO new sidebar entries are needed (the three navigation roots — استعراض الدورات, دوراتي, الحصص المباشرة — already exist in Spec 002's 15-entry student sidebar); NO header changes are needed (bell remains `<a href="notifications.html">`); NO new JS handlers are needed (the existing `data-tabs` handler from Spec 001 powers the 3-tab Live Sessions UI); NO new design tokens are needed (Spec 001 tokens cover all six pages). Verify by running `grep -c '<li>' components/student-sidebar.html` (expect: matches the 15 entries) and `grep -c 'href="notifications.html"' components/header.html` (expect: 1). Confirm `assets/js/main.js` line count ≤ 80.

**Checkpoint**: Shell baselines are confirmed unchanged. User-story page work can begin.

---

## Phase 3: User Story 1 — Browse Courses (Priority: P1) 🎯 MVP

**Goal**: Build the catalog discovery surface — hero introducing the 5 learning tracks, visual search input, 7-chip filter bar, and a responsive grid of 8 course cards (each with viewer-emphasized dual pricing per Q1 clarification + at least 3 corner badges).

**Independent Test**: Open `pages/student/browse-courses.html` from the file explorer. Without any other context, the viewer must be able to answer: "What subjects does the academy teach? What 8 courses are available right now? Who teaches the Quran level-3 course? What does it cost for an Arabic student? For a foreign student? Which courses carry Popular / New / Recommended badges? Which are private vs group?" — entirely from the page content. Filters and search are visually present but non-functional. All 5 acceptance scenarios in spec.md US1 must pass.

### Implementation for User Story 1

- [x] T003 [US1] Create `pages/student/browse-courses.html` page-level scaffold per `contracts/browse-courses.md`: `<!doctype html>` + `<html lang="ar" dir="rtl">` + Tajawal font links + `../../assets/css/output.css`; body `bg-surface-50 text-text-base font-sans antialiased`; outer wrapper `<div class="min-h-screen flex">` containing the embedded student sidebar (15 entries, **active = "استعراض الدورات"** with `is-active` + `aria-current="page"` on its `<a>`), then `<div class="flex flex-1 flex-col min-w-0">` containing the embedded header (bell as `<a href="notifications.html">`, h1 = `استعراض الدورات`, subtitle = `8 دورات متاحة عبر 5 مسارات تعليمية`) and `<main class="flex-1 overflow-y-auto p-4 md:p-6 lg:p-8"><div class="max-w-screen-xl mx-auto space-y-6">…</div></main>`. `<title>` = `استعراض الدورات — منصة إدارتي`. Inside the inner `<div>`, leave 3 placeholder `<section>` skeletons keyed by id `bc-hero`, `bc-search-filters`, `bc-grid` for tasks T004–T007. Include the `data-sidebar-backdrop`, `script src="../../assets/js/main.js"` at the body bottom.
- [x] T004 [US1] Fill `#bc-hero` (Block 1 — Hero) per `contracts/browse-courses.md`: gradient `bg-gradient-to-l from-primary-700 to-primary-900 text-white rounded-2xl p-6 md:p-8 shadow-md`; h2 `اكتشف مساراتك التعليمية في إدارتي`; description paragraph `تعلّم القرآن الكريم، اللغة العربية، الإنجليزية، الرياضيات، والدراسات الإسلامية — مع معلمين معتمدين وأسعار مرنة تناسب الطلاب العرب وغير الناطقين بالعربية.`; decorative row of 5 subject chips (each `inline-flex items-center gap-1.5 rounded-full bg-white/15 text-white px-3 py-1 text-xs font-semibold` with a small Lucide icon) covering: القرآن الكريم (`book-open`), اللغة العربية (`languages`), اللغة الإنجليزية (`globe`), الرياضيات (`calculator`), الدراسات الإسلامية (`landmark`).
- [x] T005 [US1] Fill `#bc-search-filters` (Block 2 — Search + filter bar) per `contracts/browse-courses.md` and `data-model.md §E14`: outer card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200 space-y-4`; first row contains the search `<input type="text">` with placeholder `ابحث عن دورة، معلم، أو مهارة...`, leading `search` Lucide icon (absolute positioned at start), styled `block w-full rounded-xl border border-slate-200 bg-surface-50 ps-10 pe-4 py-2.5 text-sm`; second row is `flex flex-wrap gap-2` containing exactly 7 filter chips per E14 (each rendered as `<button type="button" class="inline-flex items-center gap-1.5 rounded-full bg-surface-100 hover:bg-surface-200 text-text-base px-3.5 py-1.5 text-sm font-medium border border-slate-200">`). All controls are visual-only — no JS handler.
- [x] T006 [US1] Fill the first 4 course cards inside `#bc-grid` (Block 3 — Course grid) per `contracts/browse-courses.md` "Course card contract" using `data-model.md §E1` rows 1–4: section header `الدورات المتاحة (8)` + `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5`; cards 1-4 are: (1) `quran-l3` Recommended, group, 600 ر.س / 200 USD; (2) `quran-l1` Popular, group, 500 ر.س / 175 USD; (3) `arabic-balagha` New, group, 550 ر.س / 185 USD; (4) `arabic-nahw` no-badge, private, 700 ر.س / 250 USD. Each card MUST follow the contract: subject-themed header band, type+level pills, title (h3), description, teacher row (avatar أ.أ + الأستاذ أحمد), duration, **viewer-emphasized pricing block** (Arabic price `text-2xl font-extrabold text-accent-700` + Foreign price as smaller comparison line `text-xs text-text-muted`), `عرض التفاصيل` CTA → `course-details.html` with `chevron-left` + `rtl:rotate-180`.
- [x] T007 [US1] Append the remaining 4 course cards (rows 5–8 from `data-model.md §E1`) to `#bc-grid`: (5) `english-arabic-speakers` Popular, group, 500 ر.س / 175 USD, teacher الأستاذة سارة (use `س.م` initials + `bg-info-700` avatar); (6) `english-conversation` no-badge, private, 650 ر.س / 230 USD, teacher الأستاذ جيمس مارتن (use `ج.م` initials + `bg-success-700` avatar); (7) `math-middle` no-badge, group, 450 ر.س / 160 USD, teacher الأستاذ عمر (`ع.ع` + `bg-success-700`); (8) `islamic-sira` Recommended, group, 400 ر.س / 150 USD, teacher الشيخ عبد الله (`ش.ع` + `bg-warning-700`). All four follow the same card contract from T006. Total of 8 cards = exactly 2 rows at xl breakpoint.

**Checkpoint**: User Story 1 (MVP) is complete and independently testable. The browse-courses page now contains all required content; SC-001 is validated by inspection.

---

## Phase 4: User Story 2 — My Courses (Priority: P1)

**Goal**: Build the daily pickup surface — 3 active enrollments and 1 completed course, with each active card showing payment-model captions per Q4 clarification (full-course subscription vs per-session bundle) so the dual-payment model is unmistakable.

**Independent Test**: Open `pages/student/my-courses.html` from the file explorer. A viewer must be able to answer: "Which courses is this student actively enrolled in right now? How far along are they in each? What's their next lesson or session in each? Have they completed any courses already? Which courses use the subscription model vs per-session model?" — entirely from the page content. All 4 acceptance scenarios in spec.md US2 must pass.

### Implementation for User Story 2

- [x] T008 [US2] Create `pages/student/my-courses.html` page-level scaffold per `contracts/my-courses.md`: same shell pattern as T003 but **active sidebar entry = "دوراتي"**; `<title>` = `دوراتي — منصة إدارتي`; h1 = `دوراتي`, subtitle = `3 دورات نشطة + 1 دورة مكتملة`; `<main>` content uses `max-w-screen-xl mx-auto space-y-8` with two empty section placeholders `#mc-active` and `#mc-completed` for tasks T009–T010.
- [x] T009 [US2] Fill `#mc-active` (Active courses section) per `contracts/my-courses.md` and `research.md §R9`: header row `<h2>دوراتي النشطة <span class="text-sm font-normal text-text-muted">(3)</span></h2>` + inline-end "استعرض دورات أخرى" link → `browse-courses.html`; `grid grid-cols-1 lg:grid-cols-2 gap-5` containing 3 active course cards per the "Active course card contract": (1) **القرآن الكريم — أحكام الميم الساكنة** — payment model `اشتراك كامل`, level pill `المستوى الثالث`, teacher أ.أ + الأستاذ أحمد, progress bar at 60%, next lesson `حصة "أحكام الميم الساكنة — تطبيق عملي"` on `الثلاثاء 28 أبريل 2026 — 7:00 م`, homework count badge `3 واجبات مستحقة`, teacher note preview `"تقدّم ممتاز في تطبيق الإخفاء — ركّز على المدّ العارض في المراجعة القادمة." — الأستاذ أحمد`, primary CTA `متابعة الدورة` → `course-details.html`; (2) **اللغة العربية — البلاغة والإنشاء** — `اشتراك كامل`, level pill `المستوى الثاني`, teacher أ.س + الأستاذة سارة المنصور, progress 35%, next lesson `درس "علم البديع — المحسّنات اللفظية"` on `الأربعاء 29 أبريل 2026 — 6:00 م`, `1 واجب مستحق`, teacher note `"حسّن استخدامك للسجع في المقطع الأخير. أنصح بمراجعة فصل الجناس قبل الحصة القادمة." — الأستاذة سارة`, CTA → `course-details.html`; (3) **الدراسات الإسلامية — السيرة النبوية** — `حصص مدفوعة فردياً` + small caption `3 من 8 حصص مدفوعة مكتملة`, level pill `المستوى التمهيدي`, teacher ش.ع + الشيخ عبد الله الراشد, progress 38%, next session `حصة "غزوة بدر الكبرى — التحليل"` on `السبت 2 مايو 2026 — 4:00 م`, `0 واجبات`, teacher note `"اهتمامك بالأسئلة العميقة لافت — استمر." — الشيخ عبد الله`, CTA → `course-details.html`.
- [x] T010 [US2] Fill `#mc-completed` (Completed courses section) per `contracts/my-courses.md`: header `<h2>دورات أكملتها <span>(1)</span></h2>`; `grid grid-cols-1 lg:grid-cols-2 gap-5` containing 1 completed course card per the "Completed course card contract": **القرآن الكريم — أساسيات التلاوة والتجويد (المستوى الأول)** with `bg-success-50 border-s-4 border-success-500` styling, `award` icon chip in `bg-success-100`, teacher أ.أ + الأستاذ أحمد بن عبد الله, status badge `مكتمل` (`success` token + `check-circle-2` icon), grade pill `ممتاز` (`accent` token), completion date `أُنجزت في 20 ديسمبر 2025`, two CTAs: `عرض الشهادة` → `achievements.html` (using `award` icon) and `مراجعة الدورة` → `course-details.html`.

**Checkpoint**: User Story 2 complete. The my-courses page shows 3 active + 1 completed enrollments with payment-model captions; SC-002 is validated by inspection.

---

## Phase 5: User Story 3 — Course Details (Priority: P1)

**Goal**: Build the deep page for the Quran path Level 3 course (per Q3 clarification) — combining the marketing surface (overview, outcomes, curriculum, teacher profile, 4-combination pricing matrix) with the persona's enrolled-state sections (progress, parent confidence). 8 distinct content blocks total.

**Independent Test**: Open `pages/student/course-details.html` from the file explorer. A viewer must be able to answer: "What does this course teach? Who teaches it? How many live sessions does it include? What homework/exams come with it? Is there a certificate? What does it cost for an Arabic student paying for the full course? For a foreign student paying per session? Where is the current enrolled student in their progress?" — entirely from the page content. All 7 acceptance scenarios in spec.md US3 must pass.

### Implementation for User Story 3

- [x] T011 [US3] Create `pages/student/course-details.html` page-level scaffold per `contracts/course-details.md`: same shell pattern as T003 but **NO active sidebar entry** (drill-down page); `<title>` = `أحكام الميم الساكنة وحفظ الجزء الثلاثين — منصة إدارتي`; h1 = `أحكام الميم الساكنة وحفظ الجزء الثلاثين`, subtitle = `مسار القرآن الكريم — المستوى الثالث`; `<main>` content uses `max-w-screen-xl mx-auto space-y-6` with 8 empty section placeholders keyed by id `cd-hero`, `cd-outcomes`, `cd-curriculum`, `cd-teacher`, `cd-pricing`, `cd-includes`, `cd-progress`, `cd-parent-confidence` for tasks T012–T019.
- [x] T012 [US3] Fill `#cd-hero` (Block 1 — Hero) per `contracts/course-details.md`: gradient `bg-gradient-to-l from-primary-700 to-primary-900 text-white rounded-2xl p-6 md:p-8 shadow-md`; pills row with 3 pills (`جماعية` info-tinted, `المستوى الثالث` white-tinted, `موصى بك` accent-tinted with `thumbs-up` icon); h2 `أحكام الميم الساكنة وحفظ الجزء الثلاثين`; 2-line overview `مستوى متقدّم لإتقان أحكام الميم الساكنة الثلاثة (الإخفاء، الإدغام، الإظهار) مع حفظ كامل للجزء الثلاثين تحت إشراف معلم معتمد. مناسب لمن أنهى المستويين الأول والثاني.`; duration line `12 أسبوعاً — 3 حصص أسبوعياً` with `clock` icon; 2 CTAs: primary `اشتراك في الدورة` → `#cd-pricing` anchor (styled `bg-accent-500 text-primary-900 font-bold`), secondary `حجز حصة فردية` → `live-sessions.html` (styled `border border-white/40 text-white`).
- [x] T013 [US3] Fill `#cd-outcomes` (Block 2 — What you'll learn) per `contracts/course-details.md` and `data-model.md §E11`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5 md:p-6`; h3 `ماذا ستتعلم في هذا المستوى`; `<ul class="grid grid-cols-1 md:grid-cols-2 gap-3">` of exactly 6 outcome `<li>` rows from E11 (each row `flex items-start gap-2.5 text-sm text-text-base leading-7` with `check-circle` icon at `text-success-600`). Outcomes: (1) إتقان أحكام الميم الساكنة (إخفاء، إدغام، إظهار) في التلاوة; (2) حفظ الجزء الثلاثين بالكامل مع ضبط مخارج الحروف; (3) فهم تطبيقات المدّ بأنواعه الثلاثة (الطبيعي، المنفصل، المتّصل); (4) تحسين الأداء الصوتي والتجويد العملي تحت إشراف معلم معتمد; (5) الاستعداد الكامل للانتقال إلى المستوى الرابع (التلاوة المتقدّمة); (6) اجتياز الاختبار الشهري بتقدير لا يقل عن "ممتاز".
- [x] T014 [US3] Fill `#cd-curriculum` (Block 3 — Curriculum) per `contracts/course-details.md` and `data-model.md §E2` (mirrors Spec 002 path levels): card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5 md:p-6`; h3 `المنهج الدراسي للمسار الكامل`; `<ol class="space-y-3">` of exactly 4 level rows: (L1) `أساسيات التلاوة والتجويد` — `success` styling + `check-circle-2` + `مكتمل` badge, `12 حصة — 6 واجبات`; (L2) `مدّ الحروف وأحكام النون الساكنة` — `success` styling + `مكتمل`, `15 حصة — 8 واجبات`; (L3) `أحكام الميم الساكنة وحفظ الجزء الثلاثين` — `accent` styling with `border-2 border-accent-500` + `compass` icon + `حالياً` pill + mini progress bar at 60%, `18 حصة — 10 واجبات`; (L4) `إتقان التلاوة وتفسير قصار السور` — `slate` styling + `lock` icon at reduced contrast, `20 حصة — 12 واجبات`. Color is paired with icon and label per FR-074.
- [x] T015 [US3] Fill `#cd-teacher` (Block 4 — Teacher profile) per `contracts/course-details.md` and `data-model.md §E3`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5 md:p-6`; h3 `تعرّف على معلمك`; `flex flex-col md:flex-row items-start gap-5` containing: (a) avatar `h-20 w-20 md:h-24 md:w-24 rounded-2xl bg-primary-700 text-white inline-flex items-center justify-center text-2xl font-bold` reading `أ.أ`; (b) right column with name `الأستاذ أحمد بن عبد الله`, title `معلم القرآن الكريم — معتمد من جامعة الإمام`, 2-3 sentence bio (`خبرة 12 عاماً في تعليم القرآن الكريم وعلوم التجويد. حاصل على إجازة في رواية حفص عن عاصم. متخصّص في تبسيط القواعد العلمية للطلاب وربطها بالتطبيق العملي على المصحف الشريف.`), and a 4-tile stats grid `grid grid-cols-2 md:grid-cols-4 gap-3` showing: 12 سنة خبرة, 480 طالب تخرّج معه, 4.9 ★ متوسّط التقييم (with `star` icon at accent-700), 7 دورات يدرّسها.
- [x] T016 [US3] Fill `#cd-pricing` (Block 5 — Pricing matrix, 4 combinations) per `contracts/course-details.md` and `data-model.md §E6` + `research.md §R10`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5 md:p-6`; h3 `خيارات الدفع` + caption `اختر الخطة المناسبة لك. الأسعار شاملة الضريبة المضافة 15% للطلاب العرب.`; `grid grid-cols-1 md:grid-cols-2 gap-4` of EXACTLY 4 cards: (Cell 1) Arabic + Full-course **EMPHASIZED** with `bg-accent-50 border-2 border-accent-500` + "الأنسب لك" corner badge + `600 ر.س / شهر` at `text-3xl font-extrabold text-accent-700` + features list + "اشترك الآن" button; (Cell 2) Foreign + Full-course **COMPARISON** with `bg-white border border-slate-200` + `200 USD / شهر` at smaller `text-2xl font-bold text-text-base`; (Cell 3) Arabic + Per-session **EMPHASIZED** with `bg-white border-2 border-accent-300` + `75 ر.س / حصة` at `text-2xl font-extrabold text-accent-700` + "احجز حصة" link → `live-sessions.html`; (Cell 4) Foreign + Per-session **COMPARISON** with `bg-white border border-slate-200` + `25 USD / حصة` at smaller weight. Per Q1 clarification, the Arabic column visually dominates; the Foreign column is the comparison.
- [x] T017 [US3] Fill `#cd-includes` (Block 6 — Included sessions/homework/certificate) per `contracts/course-details.md`: outer wrapper `grid grid-cols-1 md:grid-cols-3 gap-4` containing 3 sub-cards: (Sub-card 1) `الحصص المضمنة` with `video` icon header + "عرض الكل" → `live-sessions.html` + `<ul class="space-y-2.5 text-xs">` of ≥ 2 included sessions: `حصة "أحكام الميم الساكنة — تطبيق عملي"` (الثلاثاء 28 أبريل 2026 — 7:00 م — `قادم` badge `info`) highlighted with `bg-primary-50 border border-primary-100`, and `حصة "تطبيق المدّ المنفصل والمتّصل"` (الأربعاء 30 أبريل 2026 — 7:00 م) with plain border; (Sub-card 2) `الواجبات والاختبارات` with `clipboard-list` icon header + dl with two rows: `إجمالي الواجبات: 10 واجبات`, `إجمالي الاختبارات: 3 اختبارات شهرية + اختبار نهائي`; (Sub-card 3) `الشهادة المعتمدة` with `bg-success-50 border border-success-200` + `award` icon header (success token) + body `نعم — ستحصل على شهادة معتمدة من أكاديمية إدارتي عند إتمام جميع الحصص واجتياز الاختبار النهائي بتقدير لا يقل عن "جيد جداً".`.
- [x] T018 [US3] Fill `#cd-progress` (Block 7 — Your progress, enrolled-only) per `contracts/course-details.md`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5 md:p-6`; h3 `تقدّمك في هذه الدورة`; subtitle caption `المحطّة الحالية — أحكام الميم الساكنة (تطبيق عملي)`; large progress bar at 60% with `role="progressbar" aria-valuenow="60" aria-valuemin="0" aria-valuemax="100" aria-label="تقدّمك في هذه الدورة 60%"` and visible `60%` numeric label at `text-2xl text-accent-700 font-extrabold`; CTA `متابعة الدورة من حيث توقّفت` → `my-courses.html`. Same 60% value as Spec 002 dashboard's path-progress block.
- [x] T019 [US3] Fill `#cd-parent-confidence` (Block 8 — Parent confidence snapshot, enrolled-only) per `contracts/course-details.md`: card `bg-accent-50 border-s-4 border-accent-500 rounded-2xl p-5 md:p-6 shadow-sm`; small caption `ملخص يمكن لولي الأمر متابعته` in `text-xs uppercase tracking-wide text-accent-700 font-bold` with `users` icon; `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3` of EXACTLY 4 stat tiles mirroring the Spec 002 dashboard parent-confidence block: (1) attendance 92% + معدّل الحضور + `users` icon; (2) homework 85% + الالتزام بالواجبات + `book-open-check` icon; (3) last note quote `"تقدّم ممتاز في تطبيق الإخفاء — ركّز على المدّ العارض."` + `الأستاذ أحمد` caption; (4) next evaluation `12 مايو 2026` + تقييم شهر مايو + `calendar-clock` icon. Each tile `bg-white rounded-lg p-3 border border-accent-100`. Same numeric values as Spec 002 dashboard for cross-page consistency.

**Checkpoint**: User Story 3 complete. Course-details has all 8 blocks for the Quran L3 page; SC-003 is validated (all 4 pricing combinations visible).

---

## Phase 6: User Story 4 — Live Sessions (Priority: P2)

**Goal**: Build the canonical schedule view — 3-tab UI (using existing ARIA tablist handler from Spec 001), 5-control filter bar, list/calendar view toggle, populated tabs (≥ 3 upcoming, ≥ 2 completed, ≥ 2 missed per Q2 clarification), and a static مايو 2026 calendar grid.

**Independent Test**: Open `pages/student/live-sessions.html` from the file explorer. A viewer must be able to switch tabs by clicking and verify each tab is non-empty. They must be able to identify which sessions are paid per-session vs included in subscription, and see the مايو 2026 calendar with marker dots. All 6 acceptance scenarios in spec.md US4 must pass.

### Implementation for User Story 4

- [x] T020 [US4] Create `pages/student/live-sessions.html` page-level scaffold per `contracts/live-sessions.md`: same shell pattern as T003 but **active sidebar entry = "الحصص المباشرة"**; `<title>` = `الحصص المباشرة — منصة إدارتي`; h1 = `الحصص المباشرة`, subtitle = `جدول حصصك القادمة، المكتملة، والفائتة`; `<main>` content uses `max-w-screen-xl mx-auto space-y-6` with 4 empty section placeholders keyed by id `ls-filters`, `ls-view-toggle`, `ls-tabs`, `ls-calendar` for tasks T021–T026.
- [x] T021 [US4] Fill `#ls-filters` (Block 1 — Filter bar) and `#ls-view-toggle` (Block 2 — View toggle) per `contracts/live-sessions.md`: filters card `bg-white rounded-2xl border border-slate-200 shadow-sm p-4 md:p-5` with leading `تصفية بـ` label and 5 `<select>` controls (الدورة, المعلم, نوع الحصة, الحالة, الشهر) each with realistic options matching Spec 002 personas (e.g., الدورة select includes `كل الدورات`, `القرآن الكريم — المستوى الثالث`, `اللغة العربية — البلاغة`, `الدراسات الإسلامية — السيرة`); view-toggle is a CSS-only segmented control with two `<button type="button">` items inside `inline-flex rounded-xl bg-surface-100 border border-slate-200 p-1`, first active (`bg-white shadow-sm` for "عرض القائمة"), second inactive (`text-text-muted` for "عرض التقويم"); inline-end caption `آخر تحديث: اليوم — 2:00 م` (hidden below md). All controls visual-only.
- [x] T022 [US4] Build `#ls-tabs` (Block 3 — Tabs structure) per `contracts/live-sessions.md` and `research.md §R3`: outer card `bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden`; `<div role="tablist">` with 3 `<button role="tab" type="button" aria-selected aria-controls>` entries (`tab-trigger-upcoming` aria-selected="true" + `is-active` class, `tab-trigger-completed` aria-selected="false", `tab-trigger-missed` aria-selected="false") each with the tab's Arabic label + a count-badge `inline-flex items-center justify-center min-w-[1.5rem] h-5 rounded-full` (info/success/danger tinted with counts 3/2/2); `<div data-tabpanels class="p-4 md:p-5">` containing 3 empty `<div role="tabpanel" id="tab-{key}" aria-labelledby="tab-trigger-{key}">` panels (the upcoming tabpanel visible, the other two with `hidden` class). The existing handler at `assets/js/main.js` lines 47-66 will toggle aria-selected/is-active/hidden on click — no new JS.
- [x] T023 [US4] Populate the upcoming tabpanel inside `#tab-upcoming` per `contracts/live-sessions.md` "Session row contract" and `data-model.md §E4-A` rows 1–3: `space-y-3` container with 3 session rows: (Row 1) حصة "أحكام الميم الساكنة — تطبيق عملي" — 28 أبريل 2026 — `جماعية` + `قادم` (info) — included → "عرض التفاصيل" + "انضمام" (enabled); (Row 2) حصة "تطبيق المدّ المنفصل والمتّصل" — 30 أبريل 2026 — `جماعية` + `قادم` — included → "عرض التفاصيل" + "انضمام" (visually muted, outside join window); (Row 3) حصة "مراجعة خاصة 1: تطبيق أحكام الميم الساكنة" — 1 مايو 2026 — `خاصة` + `قادم` — per-session paid + UNPAID status pill (warning) + price tag `75 ر.س / حصة · حالة الدفع: بانتظار الدفع` → "عرض التفاصيل" → `live-session-details.html` + "حجز" → `session-checkout-preview.html`. Each row uses the date-strip column (date number `text-2xl md:text-3xl font-extrabold text-primary-700` + month-year caption), vertical divider on md+, and action area on inline-end.
- [x] T024 [US4] Populate the completed tabpanel inside `#tab-completed` per `data-model.md §E4-A` rows 4–5: 2 session rows: (Row 4) حصة "حفظ سورة النبأ" — 24 أبريل 2026 — `جماعية` + `مكتمل` (success token + `check-circle-2` icon) — included — حضر — link `عرض التفاصيل`; (Row 5) حصة "تجويد سورة الواقعة" — 17 أبريل 2026 — `جماعية` + `مكتمل` — included — حضر — recording متاح (small `play` icon caption) — link `عرض التفاصيل`. Same row structure as upcoming, no join button on either.
- [x] T025 [US4] Populate the missed tabpanel inside `#tab-missed` per `data-model.md §E4-A` rows 6–7 + `research.md §R7` (Q2 clarification): 2 session rows: (Row 6) حصة "أحكام النون الساكنة — مراجعة" — 22 أبريل 2026 — `جماعية` + `فائت` (slate token + `calendar-x` icon) — included — caption `لم يحضر بسبب انقطاع الإنترنت` — links `عرض التفاصيل` + `إعادة الجدولة` (with `calendar-clock` icon, warning text-color); (Row 7) حصة "تجويد سورة الواقعة" — 15 أبريل 2026 — `جماعية` + `فائت` — included — caption `لم يحضر بدون عذر مسجّل` — links `عرض التفاصيل` + `إعادة الجدولة`. Both rows ≥ 2 satisfies FR-043. Color is paired with icon + caption per FR-074.
- [x] T026 [US4] Build `#ls-calendar` (Block 4 — Calendar view) per `contracts/live-sessions.md` and `research.md §R11`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5`; header row with h3 `عرض التقويم — مايو 2026` + 2 navigation arrow buttons (`chevron-right` for previous, `chevron-left` for next, both with `rtl:rotate-180`); days-of-week header `grid grid-cols-7 gap-1` with 7 cells (السبت/الأحد/الإثنين/الثلاثاء/الأربعاء/الخميس/الجمعة); month grid `grid grid-cols-7 gap-1` with 31 day cells for مايو 2026 (positioned correctly: 1 مايو 2026 is a Friday, so days 1-2 sit in week 1's Friday/Saturday cells). Place session-marker dots (`h-1.5 w-1.5 rounded-full bg-{color}-500`) on the dates: 1, 2, 3, 4 مايو (accent — bundle sessions), 5 مايو (danger — exam, special marker `h-2 w-2`), 6, 8, 12, 15, 22 مايو (success — group sessions). Bottom legend with 3 chips: حصة جماعية (success), حصة خاصة (accent), اختبار (danger). The calendar is rendered in the DOM alongside the tabs (FR-044 / R11) — both views always present.

**Checkpoint**: User Story 4 complete. Live-sessions has 3 tabs with populated content + filter bar + view toggle + calendar; SC-004 is validated (each tab non-empty, count badges match).

---

## Phase 7: User Story 5 — Live Session Details (Priority: P2)

**Goal**: Build the per-session command center for **bundle session #1** (مراجعة خاصة 1: تطبيق أحكام الميم الساكنة) per Q4 clarification. Single page showing upcoming-state primary content + completed-state annotation per R12.

**Independent Test**: Open `pages/student/live-session-details.html` from the file explorer. A viewer must be able to answer: what session is this, when is it, who teaches, what to prepare, did they attend (and if so what feedback), is there a recording, is it paid? — entirely from the page content. All 6 acceptance scenarios in spec.md US5 must pass.

### Implementation for User Story 5

- [x] T027 [US5] Create `pages/student/live-session-details.html` page-level scaffold per `contracts/live-session-details.md`: same shell pattern as T003 but **NO active sidebar entry**; `<title>` = `مراجعة خاصة 1: تطبيق أحكام الميم الساكنة — منصة إدارتي`; h1 = `مراجعة خاصة 1: تطبيق أحكام الميم الساكنة`, subtitle = `حصة 1 من 4 ضمن حزمة المراجعة الشهرية`; `<main>` content uses `max-w-screen-lg mx-auto space-y-6` with 6 empty section placeholders keyed by id `lsd-hero`, `lsd-actions`, `lsd-payment`, `lsd-prep`, `lsd-homework`, `lsd-completed-preview` for tasks T028–T033.
- [x] T028 [US5] Fill `#lsd-hero` (Block 1 — Hero) and `#lsd-actions` (Block 2 — Actions) per `contracts/live-session-details.md`: hero card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5 md:p-6` with: 3 pills row (`خاصة (1:1)` accent + `user` icon, `ضمن حزمة المراجعة الشهرية (1 من 4)` info + `layers` icon, `قادم` info-50 + `calendar` icon); h2 `مراجعة خاصة 1: تطبيق أحكام الميم الساكنة`; course chip `<a href="course-details.html">` reading `القرآن الكريم — المستوى الثالث` with `book-open` icon; date+duration grid (Friday 1 مايو 2026, 5:00 م إلى 6:00 م, 60 دقيقة) using `calendar` and `clock` icons; teacher row at the bottom (avatar أ.أ + الأستاذ أحمد بن عبد الله + معلم القرآن الكريم — معتمد من جامعة الإمام). Then `#lsd-actions` is `flex flex-col sm:flex-row flex-wrap gap-3` containing 3 action elements: primary `<button type="button">انضمام إلى الحصة` with `bg-primary-700 text-white rounded-xl px-5 py-3 text-sm font-bold` + `video` icon; secondary `<a href="calendar.html">إضافة للتقويم` with border + `calendar-plus` icon; tertiary `<button type="button">نسخ رابط الاجتماع` with `text-accent-700` + `link` icon.
- [x] T029 [US5] Fill `#lsd-payment` (Block 3 — Payment status block) per `contracts/live-session-details.md` and Q4+Q5 clarifications: card `bg-warning-50 border-s-4 border-warning-500 rounded-2xl p-5 shadow-sm`; left side has unpaid pill `<p class="inline-flex items-center gap-1.5 rounded-full bg-warning-100 text-warning-700 px-2.5 py-1 text-xs font-bold">` containing `alarm-clock` icon + `بانتظار الدفع — حجز حزمة المراجعة الشهرية`, body sentence `هذه الحصة جزء من حزمة المراجعة الشهرية — يلزم حجز الحزمة كاملة (4 حصص) للوصول إلى أيٍّ منها.`, and price summary `4 حصص × 75 ر.س + ضريبة 15% = 345 ر.س`; right side has CTA `<a href="session-checkout-preview.html">إتمام الدفع` styled `bg-accent-500 text-primary-900 font-bold` + `chevron-left` icon with `rtl:rotate-180`.
- [x] T030 [US5] Fill `#lsd-prep` (Block 4 — Preparation checklist) per `contracts/live-session-details.md` and `data-model.md §E12`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5`; h3 `قبل الحصة (4 خطوات)` with `list-checks` icon; `<ul class="space-y-2.5">` of 4 prep items, each row `flex items-center gap-3 py-1.5` with `<input type="checkbox" disabled>` whose `checked` attribute reflects doneState: items 1-2 checked (`راجع آيات الجزء 30 من الآية 1 إلى 20 (سورة النبأ)` and `حضّر السؤال الأول من الواجب الأخير (تطبيق إخفاء الميم)`) with strike-through + `text-text-muted`; items 3-4 unchecked (`تأكّد من جودة الكاميرا والميكروفون قبل الموعد بـ 10 دقائق` and `اختر مكاناً هادئاً مع إضاءة كافية`) with `text-text-base`.
- [x] T031 [US5] Fill `#lsd-homework` (Block 5 — Linked homework) per `contracts/live-session-details.md` and `data-model.md §E13`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5`; h3 `الواجب المرتبط` with `clipboard-list` icon (`text-warning-600`); a single linked-homework `<article>` row with `flex items-start gap-3 p-3 rounded-lg border border-slate-200`: leading 9×9 icon chip `bg-warning-50 text-warning-700` with `file-text` icon, body containing title `تطبيق أحكام الميم الساكنة في سورة النبأ` (h4 font-semibold), caption `يُستحق: الخميس 30 أبريل 2026 — الأستاذ أحمد بن عبد الله`, status badge `قيد المراجعة` (`info` token + `eye` icon), and inline-end `<a href="assignments.html">عرض الواجب` with `chevron-left` + `rtl:rotate-180`.
- [x] T032 [US5] Fill `#lsd-completed-preview` (Block 6 — Completed-state annotation) per `contracts/live-session-details.md` and `research.md §R12` (single-page upcoming + annotation strategy): outer `<aside class="bg-info-50 border border-info-200 rounded-2xl p-5">`; leading pill `معاينة الحالة بعد انتهاء الحصة` (`bg-info-100 text-info-700` + `eye` icon, uppercase tracking-wide); descriptive paragraph `عند اكتمال الحصة، يُستبدل زر "انضمام إلى الحصة" بمؤشّر حضورك (تمّ الحضور / لم تحضر)، ويظهر قسم "ملاحظات المعلم" بتعليق الأستاذ أحمد، وزر "عرض التسجيل" (أو نص "التسجيل غير متاح" إن لم يكن متوفّراً).`; below the paragraph, a `grid grid-cols-1 md:grid-cols-3 gap-3` with 3 mini-preview cards each `bg-white rounded-lg p-3 border border-slate-200`: (a) `سيتغيّر` label + `تمّ الحضور` success badge with `check-circle-2`; (b) `سيُضاف` label + italic teacher-note quote `"أداء عبد الرحمن في تطبيق إخفاء الميم كان ممتازاً..."`; (c) `سيُضاف` label + `<button>` `عرض التسجيل` with `play` icon at accent-700.

**Checkpoint**: User Story 5 complete. Live-session-details has all 6 blocks; SC-005 is validated (both upcoming and completed-state behaviors comprehensible).

---

## Phase 8: User Story 6 — Session Checkout Preview (Priority: P3)

**Goal**: Build the visual-only checkout for the **حزمة المراجعة الشهرية** 4-session bundle per Q4+Q5 clarifications. Default state shows partial balance coverage (200 ر.س applied), 145 ر.س due, and 3 active payment-method radios (FR-064 / FR-067 documentation reference for the alternative state).

**Independent Test**: Open `pages/student/session-checkout-preview.html` from the file explorer. A viewer must be able to answer: which session/bundle is being booked, what student-type rate applies, the price breakdown (base + tax = subtotal), how much of the family balance is applied, the amount due, the available payment methods. The page must include a clear "this is a prototype" notice. All 6 acceptance scenarios in spec.md US6 must pass.

### Implementation for User Story 6

- [x] T033 [US6] Create `pages/student/session-checkout-preview.html` page-level scaffold per `contracts/session-checkout-preview.md`: same shell pattern as T003 but **NO active sidebar entry**; `<title>` = `إتمام حجز حزمة المراجعة الشهرية — منصة إدارتي`; h1 = `إتمام حجز حزمة المراجعة الشهرية`, subtitle = `145 ر.س مستحق بعد خصم رصيد العائلة`; `<main>` content uses `max-w-screen-md mx-auto space-y-5` (narrower than other pages — checkout is a focused flow) with 7 empty section placeholders keyed by id `cp-intro`, `cp-summary`, `cp-student-type`, `cp-pricing`, `cp-balance`, `cp-amount-due`, `cp-payment-methods`, `cp-confirm` for tasks T034–T038.
- [x] T034 [US6] Fill `#cp-intro` (Block 1 — Prototype notice) and `#cp-summary` (Block 2 — Booking summary with all 4 bundle sessions) per `contracts/session-checkout-preview.md` and `data-model.md §E5`: intro card `bg-info-50 border border-info-200 rounded-2xl p-4 flex items-start gap-3` with `info` icon + bold `شاشة عرض تجريبية` + body `هذه صفحة معاينة لتجربة الدفع — لا يتمّ تنفيذ أيّ عملية دفع فعلية في هذا النموذج الأوّلي. الزر "تأكيد الحجز" بصري فقط.` (FR-066). Summary card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5` with header (caption `ملخص الحجز`, h2 `حزمة المراجعة الشهرية`, description, type pill `خاصة` accent-tinted), course chip → `course-details.html`, teacher row (avatar + الأستاذ أحمد), then a labeled list `الحصص المتضمّنة (4)` containing an `<ol class="space-y-2 text-xs">` with 4 numbered session rows (each `flex items-center gap-3 p-2.5 rounded-lg bg-surface-50` with circle number badge + session title + date/time): #1 الجمعة 1 مايو 2026 5:00 م, #2 السبت 2 مايو 2026 5:00 م, #3 الأحد 3 مايو 2026 5:00 م, #4 الإثنين 4 مايو 2026 5:00 م.
- [x] T035 [US6] Fill `#cp-student-type` (Block 3 — Student-type indicator) and `#cp-pricing` (Block 4 — Price calculation) per `contracts/session-checkout-preview.md` and `data-model.md §E7` + `§E9`: student-type card `bg-accent-50 border-s-4 border-accent-500 rounded-2xl p-4` with caption `تصنيف الطالب` + `user-check` icon, label `طالب عربي · يُطبَّق سعر السوق المحلّي`, and body `سعر الحصة الواحدة لك: 75 ر.س · للطلاب غير الناطقين بالعربية: 25 USD / حصة` (Q1 clarification: Arabic emphasized + Foreign as comparison). Pricing card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5` with h3 `حساب السعر` and `<dl class="text-sm divide-y divide-slate-100">` containing 3 rows: (1) base `السعر الأساسي للحزمة` → `4 حصص × 75 ر.س = 300 ر.س`; (2) tax `ضريبة القيمة المضافة (15%)` → `+ 45 ر.س`; (3) subtotal `الإجمالي قبل الرصيد` → `345 ر.س` (highlighted with `bg-surface-50` background extending to card edges via `-mx-5 px-5`). FR-062 satisfied (≥ 3 line items).
- [x] T036 [US6] Fill `#cp-balance` (Block 5 — Family balance) and `#cp-amount-due` (Block 6 — Amount due) per `contracts/session-checkout-preview.md` and `data-model.md §E8` + `§E9`: balance card `bg-info-50 border border-info-200 rounded-2xl p-5` with caption `رصيد العائلة المتاح` + `wallet` icon at info-700, large amount `750 ر.س` at `text-2xl font-extrabold text-info-700`, then a `<label>` toggle row `bg-white rounded-lg border border-info-200 p-3` with disabled+checked checkbox + label `استخدام الرصيد للدفع` + caption `سيُخصم من الرصيد: 200 ر.س · يبقى في الرصيد: 550 ر.س` (Q5 clarification: capped at 200 ر.س so amount due stays > 0). Amount-due card `bg-accent-500 text-primary-900 rounded-2xl p-5 shadow-md` with caption `المبلغ المستحق بعد خصم الرصيد`, large amount `145 ر.س` at `text-3xl md:text-4xl font-extrabold`, and inline-end Foreign comparison line `للطلاب غير الناطقين بالعربية: ~62 USD بعد خصم الرصيد` (math: 4 × 25 = 100 USD base + 15 USD VAT − ~53 USD equivalent of 200 ر.س balance ≈ 62 USD due).
- [x] T037 [US6] Fill `#cp-payment-methods` (Block 7 — Payment methods) per `contracts/session-checkout-preview.md` and `data-model.md §E10`: card `bg-white rounded-2xl border border-slate-200 shadow-sm p-5`; h3 `طريقة الدفع`; `<div class="space-y-2.5">` of EXACTLY 3 `<label>` radio-cards (FR-064): (Method 1 — PRE-SELECTED) `border-2 border-accent-500 bg-accent-50` + disabled+checked radio + VISA logo block (`bg-info-700 text-white tracking-widest` reading `VISA`) + `بطاقة ائتمانية` / `Visa تنتهي بـ 4242` + inline-end `المختارة` label; (Method 2) `border border-slate-200 hover:bg-surface-50` + disabled radio + مدى logo block (`bg-success-700`) + `مدى` / `بطاقة تنتهي بـ 8821`; (Method 3) `border border-slate-200` + disabled radio + Apple Pay logo block (`bg-text-base`) + `Apple Pay` / `مرتبط بـ iPhone — عبد الرحمن`. All radios use `name="payment-method"` and are disabled (visual state only).
- [x] T038 [US6] Fill `#cp-confirm` (Block 8 — Confirm + cancel + reminder) per `contracts/session-checkout-preview.md`: container `space-y-4`; first row `flex flex-col sm:flex-row gap-3` with primary `<button type="button">تأكيد الحجز` styled `flex-1 bg-primary-700 hover:bg-primary-800 text-white rounded-xl px-5 py-3.5 text-base font-bold` + `check-circle-2` icon, and secondary `<a href="live-session-details.html">إلغاء` styled `border border-slate-300 hover:bg-surface-50 text-text-base rounded-xl px-5 py-3.5 text-sm font-semibold`; second row is the prototype reminder `<p class="text-center text-[0.625rem] text-text-muted leading-7">` containing `بتأكيدك الحجز، أنت توافق على شروط الاستخدام وسياسة الإلغاء — <span class="text-info-700 font-semibold">شاشة بصرية فقط، لا يتمّ تنفيذ دفع فعلي.</span>` (FR-066). The `<button>` has no `onclick` / no form / no real action.

**Checkpoint**: User Story 6 complete. Session-checkout-preview has all 8 blocks; SC-006 is validated (all checkout details readable in 30 seconds).

---

## Phase 9: Polish (Cross-Cutting Validation)

**Purpose**: Final sweeps across all six pages to confirm every Success Criterion (SC-001…SC-014) passes before sign-off. These tasks read different files / concerns and are largely parallelizable.

- [x] T039 Run `npm run build:css` from repo root and verify `assets/css/output.css` size remains ≤ 80 KB via `wc -c assets/css/output.css`. The Spec 002 baseline was ~41 KB; this feature should add ≤ 8 KB (target ≤ 49 KB). If oversized, audit the `tailwind.config.js` `content` glob and prune any unused utilities. Confirm the file rebuilds successfully (no PostCSS errors).
- [x] T040 [P] Verify `lang="ar" dir="rtl"` is set on `<html>` for each of the 6 new pages. Use: `grep -L 'lang="ar" dir="rtl"' pages/student/{browse-courses,my-courses,course-details,live-sessions,live-session-details,session-checkout-preview}.html` — must list zero files (Constitution Principle III + FR-003 carry-over).
- [x] T041 [P] Verify zero `href="#"` placeholder links across all 6 pages (FR-075 / SC-007). Use: `grep -nE ' href="#"' pages/student/{browse-courses,my-courses,course-details,live-sessions,live-session-details,session-checkout-preview}.html` — must return zero matches. Same-page anchor `href="#cd-pricing"` (used in `course-details.html` hero CTA) IS allowed because the target id exists; the grep is for literal `href="#"` only.
- [x] T042 [P] Verify zero Eastern Arabic-Indic digits in all 6 page files (FR-071 / SC-008). Run `grep -nP '[٠-٩۰-۹]' pages/student/{browse-courses,my-courses,course-details,live-sessions,live-session-details,session-checkout-preview}.html` — must return zero matches. All numeric content (prices, dates, counts, percentages) MUST be in Latin digits inside Arabic copy.
- [x] T043 [P] Verify zero placeholder filler across all 6 pages (FR-077 / SC-010). Use: `grep -nEi 'lorem|Item [0-9]|TBD|Course [0-9]|Student [0-9]' pages/student/{browse-courses,my-courses,course-details,live-sessions,live-session-details,session-checkout-preview}.html` — must return zero matches.
- [x] T044 [P] Verify no JS-rendered content was introduced (FR-076 / SC-009). Use: `grep -nE 'innerHTML|createElement|outerHTML|insertAdjacentHTML|document\.write' assets/js/main.js` — must return zero matches. Also confirm `wc -l assets/js/main.js` shows ≤ 80 LOC (no new handlers added beyond the four sanctioned ones from Spec 001).
- [x] T045 [P] Verify the 3-tab Live Sessions UI works correctly using the existing handler. Open `pages/student/live-sessions.html` in a browser; click each of the 3 tabs in turn; confirm: (a) `aria-selected` toggles to `true` on the clicked tab and `false` on the others, (b) the corresponding tabpanel becomes visible (`hidden` class removed) while the others get `hidden` added, (c) the count badges in the tab labels (3 / 2 / 2) match the row counts visible in each tab. No new JS code in `main.js` — only the existing `[role="tablist"]` handler at lines 47-66.
- [x] T046 [P] Count distinct status badge usages across the 6 pages (SC-011). Verify ≥ 18 individual badge instances drawn from the Spec 001 19-badge catalog (`specs/001-frontend-foundation/data-model.md §E6`) plus the Spec 002 type chips (جماعية, خاصة, notification type chips). Expected breakdown: browse-courses corner badges (≥ 3 catalog + 8 type pills + 8 level pills), my-courses (1 مكتمل + 1 ممتاز + payment-model captions), course-details (4 curriculum status pills + 4 pricing-cell labels + 1 موصى بك hero pill + included-session قادم), live-sessions (7 type pills + 7 status pills + 2 paid-status warning pills), live-session-details (3 hero pills + 1 unpaid status + 1 homework status), checkout (2 type-chips). No invented variants.
- [x] T047 [P] Cross-page audit for FR-074 (color is never the sole indicator): walk all 6 pages and verify (a) every `<button>` and `<a>` has visible text or an `aria-label` / `<span class="sr-only">`; (b) every status indicator (badges, alerts, progress bars, level pills, attendance markers, read/unread states, milestone state) pairs color with text or icon. Specifically verify: pricing emphasis uses Arabic-bigger-bolder + accent-color (3 axes — size, weight, color) per Q1 clarification; payment-status uses border + pill text + icon; missed-session rows use slate color + `calendar-x` icon + `فائت` label.
- [x] T048 [P] Responsive audit at 375 / 768 / 1024 / 1280 px on each of the 6 pages (SC-012): verify (a) no horizontal page-level scrollbar at any breakpoint on any page, (b) the sidebar drawer (mobile burger → drawer with backdrop, body scroll locked) behaves identically to the Spec 002 baseline, (c) the browse-courses card grid collapses 1 / 2 / 3 / 4 columns at the four breakpoints, (d) the course-details pricing matrix stacks 1-col → 2-col at md, (e) the live-sessions row layout stacks legibly at < 768 px (date strip moves above the body), (f) the checkout `max-w-screen-md` keeps the page focused at all widths.
- [x] T049 [P] Cross-page navigation loop audit (SC-013): from `browse-courses.html`, click any "عرض التفاصيل" → land on `course-details.html`; from there click "حجز حصة فردية" → land on `live-sessions.html`; click any session "عرض التفاصيل" → land on `live-session-details.html`; click "إتمام الدفع" → land on `session-checkout-preview.html`; click "إلغاء" → return to `live-session-details.html`. Verify zero broken links and zero 404s along this complete loop.
- [x] T050 [P] Spec 002 → Spec 003 inbound wiring audit (SC-014): from `pages/student/dashboard.html` click "متابعة التعلم" CTA → land on `my-courses.html` (the URL was already pointing here from Spec 002's hero); from `dashboard.html` click the upcoming-session card or weekly-plan equivalent → confirm the linked target is one of the new pages. Verify zero shell drift on Spec 002 pages (no Spec 003 changes leaked into `pages/student/dashboard.html`, `learning-journey.html`, `weekly-plan.html`, `notifications.html`, `components/student-sidebar.html`, `components/header.html`, `assets/js/main.js`).
- [x] T051 Walk through the 14-row validation table in `quickstart.md §3` end-to-end and record outcomes. The feature is Done only when all 14 success criteria pass. Confirm the constitution v1.0.1 Definition of Done gates 1–12 also pass per `quickstart.md §4`.

**Final checkpoint**: Feature 003 is Done when T051 is complete with all 14 SCs and all 12 Definition-of-Done gates green.

---

## Dependencies

- **Phase 1 (Setup)**: T001 alone — read-only baseline check.
- **Phase 2 (Foundational)**: T002 alone — documentation-only sanity check; must complete before any user-story phase begins.
- **Phase 3 (US1)**: T003 → T004 → T005 → T006 → T007. All sequential because they edit the same file (`pages/student/browse-courses.html`).
- **Phase 4 (US2)**: T008 → T009 → T010. Sequential, same file.
- **Phase 5 (US3)**: T011 → T012 → T013 → T014 → T015 → T016 → T017 → T018 → T019. Sequential, same file (the Quran L3 course-details page).
- **Phase 6 (US4)**: T020 → T021 → T022 → T023 → T024 → T025 → T026. Sequential, same file.
- **Phase 7 (US5)**: T027 → T028 → T029 → T030 → T031 → T032. Sequential, same file.
- **Phase 8 (US6)**: T033 → T034 → T035 → T036 → T037 → T038. Sequential, same file.
- **Phase 9 (Polish)**: T039 must run first (CSS build); T040 / T041 / T042 / T043 / T044 / T045 / T046 / T047 / T048 / T049 / T050 are all [P] (each reads different concerns/files); T051 runs last as the sign-off gate.

User stories US2, US3, US4, US5, US6 are independent of US1 (and of each other) once Foundational completes — they don't share content. After Phase 2 completes, US1 / US2 / US3 / US4 / US5 / US6 could in principle run in parallel by separate implementers since each touches a different file. The spec recommends US1 ship first as the MVP entry surface.

## Suggested MVP scope

**MVP**: Complete Phase 1 (T001) + Phase 2 (T002) + Phase 3 (T003–T007) only. Result: a fully-built `pages/student/browse-courses.html` showing the catalog. The MVP demonstrates the dual-pricing narrative (Arabic emphasized + Foreign comparison), the catalog discovery surface, and the entry to course-details (which 404s until Phase 5 ships). Sufficient to validate the catalog design with stakeholders before committing to the full 6-page set.

## Per-story task counts

| Phase | Story | Task count |
|---|---|---|
| 1 | Setup | 1 |
| 2 | Foundational | 1 |
| 3 | US1 — Browse Courses | 5 |
| 4 | US2 — My Courses | 3 |
| 5 | US3 — Course Details | 9 |
| 6 | US4 — Live Sessions | 7 |
| 7 | US5 — Live Session Details | 6 |
| 8 | US6 — Session Checkout Preview | 6 |
| 9 | Polish | 13 |
| **Total** |  | **51** |

## Parallel execution opportunities

- **Within stories**: None — every story edits one file sequentially.
- **Across stories (after Foundational)**: All 6 user-story phases can run in parallel because each edits a distinct HTML file. Spec recommends serial implementation in priority order for narrative coherence.
- **Within Polish**: T040–T050 are all `[P]` (11 audit tasks across different concerns). Can be batched by a single agent or distributed across reviewers.

## Independent test criteria

Each user story has its own self-contained validation per the "Independent Test" sentence at the start of each phase. The story is independently testable when its single page (or group of pages for US3+US5+US6 narrative chain) opens correctly and all its acceptance scenarios pass.
