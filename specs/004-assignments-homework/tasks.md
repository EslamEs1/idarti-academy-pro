---
description: "Task list for feature 004-assignments-homework"
---

# Tasks: Assignments and Homework Frontend

**Input**: Design documents from `/specs/004-assignments-homework/`
**Prerequisites**: `plan.md` (required), `spec.md` (required), `research.md`, `data-model.md`, `contracts/`, `quickstart.md` (all present)

**Tests**: Not requested in the feature specification. Validation is manual visual review against the 15 success criteria (SC-001…SC-015) plus per-page acceptance scenarios. Audit tasks in Polish are visual checks, not unit tests.

**Organization**: Tasks are grouped by user story per `spec.md` priorities (US1 P1 → US2 P1 → US3 P1 → US4 P2 → US5 P1 → US6 P1) after a small Setup + Foundational sanity check. Within each user story, tasks edit a single HTML file sequentially — `[P]` markers are used only in the Polish phase where audit tasks read different concerns. The recommended implementation order interleaves the student- and teacher-side stories: US1 → US2 → US3 → US4 → US5 → US6 — but the teacher pages (US5/US6) are independent of the student pages (US1–US4) and can be implemented in parallel by a separate implementer once Foundational completes.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Maps to user stories from `spec.md` (US1, US2, US3, US4, US5, US6); omitted on Setup / Foundational / Polish
- All paths are relative to repo root: `/media/mekky/work/backend/idarti-academy-pro/`

## Path Conventions

This feature continues the static-site layout established by Specs 001/002/003. There is no `src/` or `tests/` tree. All deliverables are six new static HTML files — four under `pages/student/` and two under `pages/teacher/` — plus a one-time CSS rebuild.

---

## Phase 1: Setup

**Purpose**: Confirm Specs 001 / 002 / 003 baselines are still in place. No new dependencies, no new tooling.

- [x] T001 Verify Specs 001/002/003 baselines: list `assets/css/output.css` (must exist and be ~ 49 KB at start, will rebuild later); list `assets/js/main.js` (must contain only the four sanctioned handlers — drawer / dropdown / modal / tabs — and remain ≤ 80 LOC); list `pages/student/dashboard.html` line 71/486/501/516/531 (the existing inbound `<a href="assignments.html">` links from Spec 002 — must be present so this feature's pages are reachable); list `pages/teacher/dashboard.html` line 63/278 (the existing inbound `<a href="homework-review.html">` links — must be present); list `pages/student/{browse-courses,my-courses,course-details,live-sessions,live-session-details,session-checkout-preview}.html` (Spec 003 outputs intact); list `pages/student/{learning-journey,weekly-plan,notifications}.html` (Spec 002 outputs intact). If any baseline file is missing or shows drift, halt and instruct the user to complete Specs 002/003 first.

---

## Phase 2: Foundational (Shell Sanity Check — Zero Deltas)

**Purpose**: Confirm there are NO shell deltas required for this feature. Spec 002 already shipped the student sidebar with "الواجبات" → `assignments.html`; the teacher dashboard already wires "مراجعة الواجبات" → `homework-review.html`. This is a documentation-only sanity gate so the implementer doesn't accidentally edit shell partials or `main.js`.

**⚠️ CRITICAL**: T002 must complete before any user-story page work begins.

- [x] T002 Confirm zero shell deltas per `plan.md §Project Structure` and `research.md §R2`: NO new sidebar entries are needed (the student sidebar's "الواجبات" entry already exists from Spec 002 §FR-013 and now becomes the canonical anchor; the teacher sidebar's "مراجعة الواجبات" entry already exists in `pages/teacher/dashboard.html`); NO header changes are needed (bell remains `<a href="notifications.html">` on student pages and the equivalent placeholder on teacher pages); NO new JS handlers are needed (none of the six pages uses tabs / dropdowns / modals — only the existing sidebar drawer carries over); NO new design tokens are needed (Spec 001 tokens cover all six pages). Verify by running `grep -c 'href="assignments.html"' pages/student/dashboard.html` (expect ≥ 5: 1 sidebar + 4 homework block); `grep -c 'href="homework-review.html"' pages/teacher/dashboard.html` (expect ≥ 2: 1 sidebar + 1 inline link); `wc -l assets/js/main.js` (≤ 80 LOC). Confirm no edits planned to `tailwind.config.js`, `assets/css/input.css`, or any embedded sidebar/header partial.

**Checkpoint**: Shell baselines are confirmed unchanged. User-story page work can begin.

---

## Phase 3: User Story 1 — Student Assignments Index (Priority: P1) 🎯 MVP

**Goal**: Build the canonical homework surface — page header + 5 summary cards + 9 status filter chips + 4 secondary filter dropdowns + search input + a responsive list of 9 assignment rows (table on desktop, stacked cards on mobile) covering every status.

**Independent Test**: Open `pages/student/assignments.html` from the file explorer. Without any other context, the viewer must be able to answer: "How many assignments are required of me right now? How many are awaiting review? Which one needs revision? Which is overdue? Who teaches each? What grade did I get on the accepted ones?" — entirely from the page content. Filters and search are visually present but non-functional. All 6 acceptance scenarios in spec.md US1 must pass.

### Implementation for User Story 1

- [x] T003 [US1] Create `pages/student/assignments.html` page-level scaffold per `contracts/assignments.md`: `<!doctype html>` + `<html lang="ar" dir="rtl">` + Tajawal font links + `../../assets/css/output.css`; body `bg-surface-50 text-text-base font-sans antialiased`; outer wrapper `<div class="min-h-screen flex">` containing the embedded student sidebar (15 entries from Spec 002, **active = "الواجبات"** with `is-active` + `aria-current="page"` on its `<a href="assignments.html">`), then `<div class="flex flex-1 flex-col min-w-0">` containing the embedded header (bell as `<a href="notifications.html">`) and `<main class="flex-1 overflow-y-auto p-4 md:p-6 lg:p-8"><div class="max-w-screen-xl mx-auto space-y-6">…</div></main>`. `<title>` = `واجباتي — منصة إدارتي`. Inside the inner `<div>`, leave 4 placeholder `<section>` skeletons keyed by id `ai-header`, `ai-summary`, `ai-filters`, `ai-list` for tasks T004–T007. Include the `data-sidebar-backdrop` element and `<script src="../../assets/js/main.js">` at the body bottom.
- [x] T004 [US1] Fill `#ai-header` (Page header) per `contracts/assignments.md`: `<h1 class="text-2xl md:text-3xl font-bold mb-2">واجباتي</h1>` + subtitle paragraph `تابع كل واجباتك في مكان واحد — مطلوب، مسلَّم، تحت المراجعة، تحتاج تعديل، متأخرة.` (text-text-muted). Then fill `#ai-summary` (5 summary cards) per `data-model.md §E9 student-side`: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4` containing exactly 5 cards in this order: (1) واجبات مطلوبة — count 3 — accent tint with `clipboard-list` icon; (2) تم التسليم — 4 — info tint with `send` icon; (3) بانتظار المراجعة — 1 — info tint with `eye` icon; (4) تحتاج تعديل — 1 — warning tint with `alert-triangle` icon; (5) متأخرة — 1 — danger tint with `alert-octagon` icon. Each card uses pattern from contract: `bg-{tint}-50 border border-{tint}-200 rounded-2xl p-4 md:p-5 flex items-start gap-3`, leading rounded-icon block, large `text-2xl font-bold` count, sm-text label.
- [x] T005 [US1] Fill `#ai-filters` (Search + status chips + secondary filters) per `contracts/assignments.md` and `data-model.md §E7 / §E10`: outer card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200 space-y-4`. (a) Search row: relative container with absolute leading `search` Lucide icon + `<input type="text" name="ai-search" placeholder="ابحث عن واجب أو دورة..." class="w-full ps-10 pe-4 py-2.5 rounded-xl border border-slate-300">`. (b) Status chip row: `<div role="tablist" aria-label="تصفية حسب الحالة" class="flex flex-wrap gap-2">` containing exactly 9 `<button type="button" role="tab">` chips per E7 student-side: الكل (selected — `aria-selected="true"` + `is-active` styling, accent-tint), جديد (accent + `bell` icon), مطلوب التسليم (slate + `clock`), تم التسليم (info + `send`), قيد المراجعة (info + `eye`), مقبول (success + `check-circle`), يحتاج تعديل (warning + `alert-triangle`), متأخر (danger + `alert-octagon`), مرفوض (danger + `x-circle`). All chips visual-only; no JS handler attaches. (c) Secondary filter row: `grid grid-cols-2 lg:grid-cols-4 gap-3` containing 4 labeled `<select>` controls — الدورة, المعلم, الشهر, الحالة — with the option lists from `data-model.md §E10 student-side`. All visual-only.
- [x] T006 [US1] Fill `#ai-list` desktop table per `contracts/assignments.md` and `research.md §R8`: section header `<h2 class="text-base font-semibold mb-3">قائمة الواجبات (9)</h2>`. Render `<table class="hidden md:table w-full text-sm border-collapse">` with `<thead class="bg-slate-50 text-text-muted">` carrying 7 columns (الواجب / الدورة / المعلم / تاريخ التسليم / الحالة / الدرجة / إجراء) and `<tbody class="divide-y divide-slate-100">` containing exactly 9 `<tr>` rows from R8 inventory: (1) `تطبيق أحكام الميم الساكنة من سورة البقرة (الآيات 1-10)` / القرآن المستوى الثالث / الأستاذ أحمد العمري / 10 مايو 2026 (caption `بعد يومين`) / status pill يحتاج تعديل / `65 / 100` / "عرض التفاصيل" → `assignment-details.html` + "إعادة التسليم" → `submit-assignment.html`; (2) `تلاوة سورة الضحى مع تطبيق أحكام التجويد` / Quran L3 / أ.أحمد / 9 مايو 2026 (caption `غداً`) / pill قيد المراجعة / `—` / "عرض التفاصيل"; (3) `تسميع الدرس السابق + كتابة 5 أحكام تعلمتها` / Quran L3 / أ.أحمد / 9 مايو 2026 (caption `غداً`) / pill جديد (accent + `bell`) / `—` / "عرض التفاصيل" + "تسليم" → `submit-assignment.html`; (4) `حفظ سورة الفجر كاملة` / Quran L3 / أ.أحمد / 12 مايو 2026 (caption `بعد 4 أيام`) / pill مطلوب التسليم (slate + `clock`) / `—` / "عرض التفاصيل" + "تسليم"; (5) `كتابة فقرة عن آداب التلاوة` / اللغة العربية — التعبير / الأستاذة سلمى البلوي / 11 مايو 2026 (caption `بعد 3 أيام`) / pill مطلوب التسليم / `—` / "عرض التفاصيل" + "تسليم"; (6) `كتابة ملخص لدرس أحكام النون الساكنة` / Quran L3 / أ.أحمد / 30 أبريل 2026 (caption `قبل 8 أيام`) / pill مقبول / `92 / 100` / "عرض التفاصيل" + "عرض الملاحظات" → `submission-feedback.html`; (7) `حلّ تمارين الإملاء — الدرس الرابع` / اللغة العربية — التعبير / أ.سلمى / 23 أبريل 2026 / pill مقبول / `88 / 100` / "عرض التفاصيل" + "عرض الملاحظات"; (8) `حفظ آخر 5 آيات من سورة البقرة` / Quran L3 / أ.أحمد / 2 مايو 2026 (caption `متأخر بـ 6 أيام`) / pill متأخر (danger + `alert-octagon`) / `—` / "عرض التفاصيل" + "تسليم"; (9) `تسجيل تلاوة سورة الإخلاص (محاولة سابقة)` / القرآن المستوى الثاني / أ.أحمد / 25 مارس 2026 / pill مرفوض (danger + `x-circle`) / `35 / 100` / "عرض التفاصيل" + "عرض الملاحظات". Each row uses `<td class="px-3 py-3 align-top">`. Teacher chip cell uses an avatar (e.g., `أ.أ` for أحمد العمري in `bg-primary-700 text-white rounded-full h-7 w-7`) + name. Status pill uses tints from E7 student-side mapping. Color paired with text + icon throughout.
- [x] T007 [US1] Fill `#ai-list` mobile card list (mirror of T006 rows) per `contracts/assignments.md`: render `<div class="md:hidden space-y-3">` containing 9 `<article class="bg-white rounded-2xl border border-slate-200 p-4 shadow-sm space-y-3">` cards using the contract's mobile card pattern. Each card carries: title (h3 text-sm font-semibold), course chip + teacher chip with avatar + name (text-xs muted), due date strip (relative caption + absolute date), status pill + grade (flex justify-between row), CTA cluster (1-2 inline links matching the row's status mapping from T006). Same 9 rows in same order; same content as the desktop table; only the layout differs. The `hidden md:table` / `md:hidden space-y-3` toggle ensures one is visible at any breakpoint with no JS state.

**Checkpoint**: User Story 1 (MVP) is complete and independently testable. The assignments index renders all 9 rows + 5 summary cards + 9 chips + 4 dropdowns; SC-001 is validated by inspection.

---

## Phase 4: User Story 2 — Assignment Details (Priority: P1)

**Goal**: Build the deep view of the featured Quran resubmission case — meta-strip + required-task + teacher-instructions + Quran brief (4 sub-blocks) + 5 attached materials + submission history (1 prior attempt) + sticky action area with the إعادة التسليم primary CTA.

**Independent Test**: Open `pages/student/assignment-details.html` from the file explorer. A viewer must be able to answer: "What sura and ayah range is being asked? Which tajweed rule did the teacher highlight? At least 3 common mistakes to avoid? Prior grade 65/100? Next action = إعادة التسليم?" — entirely from the page content. All 7 acceptance scenarios in spec.md US2 must pass.

### Implementation for User Story 2

- [x] T008 [US2] Create `pages/student/assignment-details.html` page-level scaffold per `contracts/assignment-details.md`: same shell pattern as T003 with **active sidebar entry = "الواجبات"**; `<title>` = `تطبيق أحكام الميم الساكنة من سورة البقرة (الآيات 1-10) — واجباتي — منصة إدارتي`; `<main>` content uses `max-w-screen-lg mx-auto space-y-6`. Inside the inner wrapper, place 7 placeholder `<section>` skeletons keyed by id `ad-header`, `ad-task`, `ad-instructions`, `ad-quran-brief`, `ad-materials`, `ad-history`, `ad-actions` for tasks T009–T015.
- [x] T009 [US2] Fill `#ad-header` (Breadcrumb + h1 + meta-strip) per `contracts/assignment-details.md`: breadcrumb nav `<nav aria-label="breadcrumb" class="text-xs text-text-muted mb-3">` with `<a href="assignments.html">واجباتي</a>` → `/` separator → `<span aria-current="page">تطبيق أحكام الميم الساكنة...</span>`. Then `<h1 class="text-xl md:text-2xl font-bold mb-4">تطبيق أحكام الميم الساكنة من سورة البقرة (الآيات 1-10)</h1>`. Then `<div id="ad-meta" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3">` containing exactly 6 cells per FR-021: (1) الدورة → القرآن الكريم — المستوى الثالث; (2) المعلم → avatar أ.أ + الأستاذ أحمد العمري; (3) تاريخ الإنشاء → 2 مايو 2026; (4) تاريخ التسليم → 10 مايو 2026; (5) الحالة → status pill يحتاج تعديل (warning tint + `alert-triangle` icon); (6) الدرجة الحالية → `65 / 100`. Each cell `bg-slate-50 rounded-xl p-3 md:p-4` with a small label + the value.
- [x] T010 [US2] Fill `#ad-task` (Required task) and `#ad-instructions` (Teacher instructions) per `contracts/assignment-details.md`: task card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` with h2 `المطلوب منك`, paragraph `عليك حفظ الآيات 1 إلى 10 من سورة البقرة، ثم تسجيل تلاوتك بصوت واضح مع تطبيق أحكام الميم الساكنة الثلاثة (الإخفاء الشفوي، الإدغام الشفوي، الإظهار الشفوي) في كل موضع. هذا الواجب جزء من تثبيت أحكام الجزء الأول من سورة البقرة قبل اختبار المنتصف.`, then `<ol class="list-decimal ps-6 space-y-2 text-sm">` with 4 sub-tasks: (1) احفظ الآيات 1 إلى 10 من سورة البقرة; (2) سجّل تلاوتك بصوت واضح في مكان هادئ — مدة التسجيل بين 60 و 90 ثانية; (3) طبّق أحكام الميم الساكنة في كل موضع — الإخفاء الشفوي، الإدغام الشفوي، الإظهار الشفوي; (4) راجع التسجيل قبل الرفع وتأكد من جودة الصوت. Then instructions card `bg-primary-50 border-r-4 border-primary-500 rounded-2xl p-5 md:p-6` with h2 `تعليمات المعلم` followed by the 4-sentence teacher quote from `contracts/assignment-details.md` (about أحكام الميم الساكنة, the specific ayah examples, recording quality, resubmission expectations) + a teacher avatar + name caption beneath.
- [x] T011 [US2] Fill `#ad-quran-brief` (Quran brief) per `contracts/assignment-details.md` and `data-model.md §E3` + `research.md §R7`: card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200`; h2 `البريف القرآني` with `book-open` icon at `text-primary-700`; `grid grid-cols-1 md:grid-cols-2 gap-5` containing 4 labeled blocks: (a) **نطاق الحفظ والمراجعة** — `سورة البقرة، الآيات 1 إلى 10` + caption `تقريباً 65 كلمة — مع التركيز على ضبط الإعراب والوقف.`; (b) **تركيز التجويد** — heading `أحكام الميم الساكنة` + numbered list of 3 rules from R7 (الإخفاء الشفوي / الإدغام الشفوي / الإظهار الشفوي) each with a one-line explanation; (c) **أخطاء شائعة يجب تجنّبها** — `<ul class="list-disc ps-6 space-y-1.5 text-sm">` with the 4 bullets from R7 (الخلط بين الإخفاء والإدغام عند الباء؛ ترك الغنّة عند الإدغام؛ المبالغة في الإظهار؛ إقلاب الميم نوناً عند الإخفاء); (d) **طريقة التسجيل المطلوبة** — body `مكان هادئ، 60-90 ثانية، صوت واضح، التزام بمخارج الحروف.` Each block uses `bg-slate-50 rounded-xl p-4` styling.
- [x] T012 [US2] Fill `#ad-materials` (Attached materials — 5 tiles per E2) per `contracts/assignment-details.md` and `research.md §R13`: section header h2 `الملفات والمراجع المرفقة` + `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` containing exactly 5 `<a>` attachment tiles using the tile pattern from the contract: (1) **PDF** — `bg-rose-50 text-rose-700` icon block, name `ملخص أحكام الميم الساكنة.pdf`, meta `PDF — 1.4 MB`, `href="#planned"` action `تنزيل الملف`; (2) **Audio** — `bg-violet-50 text-violet-700`, name `نموذج تلاوة من الشيخ المنشاوي.mp3`, meta `MP3 — مدة 02:18`, `href="#planned"` action `تشغيل` (audio play stub per `research.md §R10`); (3) **Video** — `bg-amber-50 text-amber-700`, name `شرح الإخفاء الشفوي والإدغام الشفوي.mp4`, meta `MP4 — مدة 06:45`, action `تشغيل`; (4) **Image** — `bg-emerald-50 text-emerald-700`, name `جدول أحكام الميم الساكنة.png`, meta `PNG — 1280×720`, action `عرض`; (5) **External link** — `bg-sky-50 text-sky-700` with `external-link` icon, name `تطبيقات على Quran.com — سورة البقرة (الآيات 1-10)`, meta `quran.com`, action `فتح الرابط`, `href="https://quran.com/2"` + `target="_blank"` + `rel="noopener noreferrer"` per `research.md §R11`. Each tile is `block bg-white rounded-2xl border border-slate-200 hover:border-{tint}-300 hover:shadow-sm p-4 group transition-all`.
- [x] T013 [US2] Fill `#ad-history` (Submission history — 1 prior attempt) per `contracts/assignment-details.md`: card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200`; h2 `سجل التسليمات`; `<ol class="relative ps-8 border-s-2 border-slate-200 space-y-6">` containing 1 attempt entry per FR-026: timeline-circle pin (`bg-warning-100 text-warning-700 ring-4 ring-white` with `alert-triangle` icon), inner `bg-white rounded-2xl border border-slate-200 p-4` carrying: title `المحاولة 1 من 3 محاولات مسموح بها` + timestamp `7 مايو 2026 — الساعة 09:14 ص`, file row with audio play-stub button (per R10) + filename `تسجيل-الميم-الساكنة-المحاولة-1.mp3` + duration `01:18`, status pill `يحتاج تعديل` + grade `65 / 100`, and link `<a href="submission-feedback.html" class="text-xs font-medium text-accent-700">عرض ملاحظات المعلم →</a>`. Append annotation paragraph below the `<ol>`: `<p class="text-xs text-text-muted mt-4 italic">* بعد إعادة التسليم، ستظهر المحاولة الثانية كبطاقة جديدة في أعلى هذه القائمة، وتنخفض المحاولة الأولى إلى الأسفل.</p>`.
- [x] T014 [US2] Fill `#ad-actions` (Sticky action area) per `contracts/assignment-details.md` and FR-027: `<section id="ad-actions" class="sticky bottom-4 bg-white rounded-2xl p-4 md:p-5 shadow-lg border border-slate-200 flex flex-wrap items-center gap-3">` containing 3 elements in order: (a) primary `<a href="submit-assignment.html" class="…btn-primary…">إعادة التسليم</a>` with `send` icon at start; (b) secondary `<a href="#ad-materials" class="…btn-secondary…">تنزيل ملفات الواجب</a>` with `download` icon; (c) tertiary `<a href="assignments.html" class="…btn-tertiary…">العودة لقائمة الواجبات</a>` plain text with `arrow-right` icon (visual return arrow given RTL).

**Checkpoint**: User Story 2 complete. The assignment-details page has all 7 blocks fully populated. SC-002 is validated by inspection.

---

## Phase 5: User Story 3 — Submit Assignment (Priority: P1)

**Goal**: Build the action surface — assignment summary pinned card + 7 submission method blocks (audio active and pre-expanded; 6 muted) + note-to-teacher textarea + 5-item pre-submit checklist (4 pre-checked + 1 unchecked) + visual Submit/Save/Cancel footer.

**Independent Test**: Open `pages/student/submit-assignment.html` from the file explorer. A viewer must be able to count ≥ 7 distinct submission method blocks each with its own UI, see the audio block pre-expanded with sample upload + duration, check the pre-submit checklist showing both pre-checked (4) and unchecked (1) states, and locate the Submit button at the bottom. All 6 acceptance scenarios in spec.md US3 must pass.

### Implementation for User Story 3

- [x] T015 [US3] Create `pages/student/submit-assignment.html` page-level scaffold per `contracts/submit-assignment.md`: same shell pattern as T003 with **active sidebar entry = "الواجبات"**; `<title>` = `تسليم الواجب: تطبيق أحكام الميم الساكنة — منصة إدارتي`; `<main>` content uses `max-w-screen-lg mx-auto space-y-6` with 5 placeholder `<section>` skeletons keyed by id `sa-summary`, `sa-methods`, `sa-note`, `sa-checklist`, `sa-actions` for tasks T016–T019. The page-level `<h1>` lives inside `#sa-summary` (the pinned summary card) — there is no top-of-page hero.
- [x] T016 [US3] Fill `#sa-summary` (Pinned assignment summary) per `contracts/submit-assignment.md`: `bg-primary-50 border border-primary-200 rounded-2xl p-5 md:p-6` containing: leading link `<a href="assignment-details.html" class="text-xs text-accent-700 hover:underline">← العودة لتفاصيل الواجب</a>`, h1 `تطبيق أحكام الميم الساكنة من سورة البقرة (الآيات 1-10)` (text-base md:text-lg font-semibold mt-1), meta strip with course chip / teacher chip (avatar أ.أ + الأستاذ أحمد العمري) / due date `التسليم قبل 10 مايو 2026`, and an inline-end attempt pill `<span class="…attempt pill…">المحاولة 2 من 3</span>`.
- [x] T017 [US3] Fill `#sa-methods` (7 submission method blocks per E6) per `contracts/submit-assignment.md` and `research.md §R10`: section header h2 `طريقة التسليم` + helper caption `اختر طريقة التسليم المناسبة لهذا الواجب. الطرق التي لا تنطبق على هذا الواجب تظهر كمكتومة.` + `<div class="space-y-4">` containing 7 method blocks. **Block 1 (the active one — audio-upload)** uses `bg-white rounded-2xl p-5 md:p-6 shadow-md border-2 border-accent-300 ring-1 ring-accent-100` with header (mic icon + label `رفع تسجيل صوتي` + helper `حدّ أقصى 5 MB — MP3 / M4A / WAV — مدة 60-90 ثانية` + success pill `تم الرفع`), pre-uploaded preview row (`bg-slate-50 rounded-xl p-4` with audio play-stub button per R10 + filename `تسجيل-الميم-الساكنة-المحاولة-2.mp3` + meta `1.2 MB · 01:14 / 01:30` + trash button), waveform placeholder (`flex items-end gap-0.5 h-12` with ~40 small `<span>` bars at varying Tailwind h-* heights — visual only, `aria-hidden="true"`), and re-record/upload affordances. **Blocks 2-7 (muted)** each use `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200 opacity-70` with `data-applies="false"` + a "غير مطلوب لهذا الواجب" caption: نص الإجابة (textarea), رفع ملف (drag-drop zone), رفع صورة (drop zone + camera icon), رفع فيديو (drop zone + max-duration hint), رابط خارجي (URL input), تأكيد الإنجاز (labeled checkbox). Each muted block still shows its full UI to demonstrate the method.
- [x] T018 [US3] Fill `#sa-note` (Note to teacher) and `#sa-checklist` (Pre-submit checklist) per `contracts/submit-assignment.md`: note card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` containing a `<label for="sa-note-text">` reading `ملاحظة للمعلم <span class="text-xs font-normal text-text-muted">(اختياري)</span>` + a `<textarea id="sa-note-text" rows="4" placeholder="اكتب ملاحظاتك أو أسئلتك للمعلم — مثلاً: 'كنت غير متأكد من الإخفاء عند الباء في الآية الثالثة'..." class="w-full rounded-xl border border-slate-300 p-3"></textarea>` + caption `سيراها المعلم مع تسليمك.`. Then checklist card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` containing h2 `قبل التسليم — قائمة التحقق` + `<ul class="space-y-3">` of exactly 5 items per FR-034 (each `<li class="flex items-start gap-3"><input type="checkbox" id="chk-N" {checked}><label for="chk-N">…</label></li>`): items 1-4 carry the `checked` attribute, item 5 does not. Item copy: (1) راجعت الآيات قبل التسجيل; (2) التسجيل في مكان هادئ بدون تشويش; (3) صوتي واضح وغير مكتوم; (4) طبّقت أحكام الميم الساكنة في كل موضع; (5) استمعت لتسجيلي مرّتين قبل الرفع.
- [x] T019 [US3] Fill `#sa-actions` (Action area + prototype notice) per `contracts/submit-assignment.md` and FR-035: card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` containing two rows: (a) action row `flex flex-wrap items-center gap-3 mb-3` with primary `<button type="button" class="…btn-primary…"><svg>…send icon…</svg> تسليم الواجب</button>` (no `onclick`, no form action), secondary `<a href="#sa-summary" class="…btn-secondary…"><svg>…save icon…</svg> حفظ كمسودة</a>` (visual stub anchor), tertiary `<a href="assignment-details.html" class="…btn-tertiary…">إلغاء</a>`; (b) prototype notice row: `<p class="text-xs text-warning-700 bg-warning-50 border border-warning-200 rounded-lg px-3 py-2 inline-flex items-center gap-2"><svg>…info icon…</svg> هذا نموذج تجريبي — لا يتم رفع التسليم فعلياً.</p>`.

**Checkpoint**: User Story 3 complete. The submit-assignment page has the pinned summary, all 7 method blocks (audio active + 6 muted), note textarea, 5-item checklist, and visual action footer; SC-003 is validated by inspection.

---

## Phase 6: User Story 4 — Submission Feedback (Priority: P2)

**Goal**: Build the post-submission feedback surface — meta-strip + teacher feedback quote + strengths/improvements panels + Quran evaluation rubric (4 axes + R4 independence caption + mistakes/ayahs summary) + resubmission panel + attempts timeline.

**Independent Test**: Open `pages/student/submission-feedback.html` from the file explorer. A viewer must be able to identify the four Quran-axis scores (85/60/75/70), the mistakes count (5), the ayahs needing review (3, 4, 7), the resubmission deadline, and the attempts remaining (2 من 3). All 6 acceptance scenarios in spec.md US4 must pass.

### Implementation for User Story 4

- [x] T020 [US4] Create `pages/student/submission-feedback.html` page-level scaffold per `contracts/submission-feedback.md`: same shell pattern as T003 with **active sidebar entry = "الواجبات"**; `<title>` = `ملاحظات المعلم: تطبيق أحكام الميم الساكنة — منصة إدارتي`; `<main>` content uses `max-w-screen-lg mx-auto space-y-6` with 6 placeholder `<section>` skeletons keyed by id `sf-header`, `sf-feedback`, `sf-strengths-improvements`, `sf-quran`, `sf-resubmit`, `sf-attempts` for tasks T021–T025.
- [x] T021 [US4] Fill `#sf-header` (Breadcrumb + h1 + meta-strip) per `contracts/submission-feedback.md` and FR-040 / FR-041: breadcrumb nav with 3 segments (`<a href="assignments.html">واجباتي</a>` → `<a href="assignment-details.html">تطبيق أحكام الميم الساكنة...</a>` → `<span aria-current="page">ملاحظات المعلم</span>`); `<h1>ملاحظات المعلم على: تطبيق أحكام الميم الساكنة من سورة البقرة (الآيات 1-10)</h1>`; meta-strip `<div id="sf-meta" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3">` with 6 cells: (1) الدورة → القرآن المستوى الثالث; (2) المعلم → avatar أ.أ + أحمد العمري; (3) الحالة → status pill يحتاج تعديل (warning + `alert-triangle`); (4) الدرجة → numeric `65 / 100` + small `<div class="h-2 bg-slate-200">` progress bar at 65% width; (5) تاريخ التقييم → 7 مايو 2026 — 14:32 م; (6) المحاولة → 1 من 3.
- [x] T022 [US4] Fill `#sf-feedback` (Teacher feedback quote) and `#sf-strengths-improvements` (Strengths + Improvements panels) per `contracts/submission-feedback.md` and FR-042 / FR-043: feedback card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200 border-r-4 border-r-primary-500` with header (avatar أ.أ + name + date `7 مايو 2026 — الساعة 14:32 م`) + `<blockquote class="text-sm md:text-base leading-8 ps-4 border-s-2 border-primary-200">` containing the 5-sentence teacher feedback from the contract (about إظهار being good in آية 1, إخفاء needing work at لِمَنْ آمَنَ in آية 3, Ghunnah short at آية 7, recommendation to listen to المنشاوي recording, encouragement). Then strengths/improvements section `grid grid-cols-1 md:grid-cols-2 gap-5`: (a) success panel `bg-success-50 border border-success-200 rounded-2xl p-5` with h2 `نقاط قوّتك` + `<ul class="space-y-3">` of 4 bullets (each `flex items-start gap-3` with `check-circle` icon at `text-success-600`): الإظهار الشفوي ممتاز في آيات 1/2/5, الحفظ مكتمل بدون أخطاء, جودة التسجيل ممتازة, الالتزام بمدة التسجيل; (b) warning panel `bg-warning-50 border border-warning-200 rounded-2xl p-5` with h2 `نقاط للتحسين` + `<ul class="space-y-3">` of 3 bullets with `alert-circle` icon at `text-warning-600`: الإخفاء الشفوي عند الباء في آية 3, الغنّة في الإدغام قصيرة, توقفات قصيرة تكسر الطلاقة.
- [x] T023 [US4] Fill `#sf-quran` (Quran evaluation block) per `contracts/submission-feedback.md` and FR-044 + `research.md §R4` (Q1 clarification): card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200`; h2 `تقييم الأداء القرآني`; **independence caption** (R4) immediately below h2 — `<p class="text-xs text-text-muted leading-7 mb-5 italic">المحاور أعلاه تفصيلية لأدائك في كل مهارة. التقييم العام (65 / 100) في الأعلى تقدير شامل من المعلم يأخذ في الاعتبار جودة التسجيل، الالتزام، وسلوكيات الأدب القرآني إلى جانب المهارات.</p>` — must contain the substring `تقدير شامل` per FR-044 verification. Then `grid grid-cols-1 md:grid-cols-2 gap-5` containing 4 axis cards from `data-model.md §E5`: (a) **الحفظ** 85 — `bg-success-500` bar tint + comment `حفظ ممتاز للآيات — كلمات قليلة جداً تحتاج تثبيتاً.`; (b) **التجويد** 60 — `bg-warning-500` bar + comment `تطبيق الإخفاء الشفوي يحتاج تركيزاً أكبر — خاصة عند الالتقاء بالباء.`; (c) **النطق** 75 — `bg-info-500` bar + comment `النطق واضح بشكل عام — المخارج الجانبية تحتاج تعديلاً.`; (d) **الطلاقة** 70 — `bg-info-500` bar + comment `الطلاقة جيدة — حاول تقليل الوقفات الطويلة بين الآيات.` Each axis card `bg-slate-50 rounded-xl p-4 md:p-5` with header (icon + axis name + bold score), `h-2 bg-slate-200 rounded-full` progress bar with `inline-size: {score}%` fill, and comment paragraph. Below the grid, summary lines `grid grid-cols-1 md:grid-cols-2 gap-3 mt-5 pt-5 border-t border-slate-100`: (1) `<svg>…alert-octagon…</svg> عدد الأخطاء المرصودة: 5 أخطاء`; (2) `<svg>…book-marked…</svg> آيات تحتاج إلى مراجعة: الآيات 3 و 4 و 7`.
- [x] T024 [US4] Fill `#sf-resubmit` (Resubmission panel) per `contracts/submission-feedback.md` and FR-045: panel `bg-warning-50 border-2 border-warning-300 rounded-2xl p-5 md:p-6` containing: header row with large `rotate-ccw` icon block (`bg-warning-100 text-warning-700`) + h2 `المعلم طلب إعادة تسليم هذا الواجب` + caption `راجع نقاط التحسين أعلاه ولاحظ الآيات التي تحتاج تركيزاً قبل المحاولة الثانية.`; deadline + attempts `grid grid-cols-1 md:grid-cols-2 gap-3 mb-5` with 2 white-bg sub-cards: (a) `calendar-clock` icon + label `الموعد النهائي لإعادة التسليم` + value `10 مايو 2026 — قبل 23:59`; (b) `repeat` icon + label `المحاولات المتبقية` + value `لديك محاولتان متبقيتان من أصل 3`; primary CTA `<a href="submit-assignment.html" class="…btn-primary…"><svg>…send…</svg> إعادة التسليم الآن</a>`.
- [x] T025 [US4] Fill `#sf-attempts` (Attempts timeline) per `contracts/submission-feedback.md` and FR-046: card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200`; h2 `محاولاتك السابقة`; `<ol class="relative ps-8 border-s-2 border-slate-200 space-y-5">` containing 1 `<li>` for the current attempt: timeline-circle pin (`bg-warning-100` ring), inner `bg-warning-50 rounded-2xl border border-warning-200 p-4` with title `المحاولة 1 — الحالية` + status pill `يحتاج تعديل` + meta strip `7 مايو 2026 — 09:14 ص · تسجيل-الميم-الساكنة-المحاولة-1.mp3 · 65 / 100`. Below the `<ol>` add annotation paragraph `<p class="text-xs text-text-muted mt-4 italic">* بعد إتمام المحاولة الثانية، ستظهر أعلى هذه القائمة وتنخفض المحاولة الأولى إلى الأسفل.</p>`.

**Checkpoint**: User Story 4 complete. The submission-feedback page renders all 6 blocks including the Q1 independence caption; SC-004 is validated by inspection.

---

## Phase 7: User Story 5 — Teacher Homework Review Queue (Priority: P1)

**Goal**: Build the teacher's primary daily surface — page header + 4 summary cards + 4 secondary filter dropdowns + search input + 8-row queue (table on desktop, stacked cards on mobile) covering every status. Row 1 is the persona's pending Quran submission so the cross-page narrative reconciles.

**Independent Test**: Open `pages/teacher/homework-review.html` from the file explorer. A viewer must be able to answer: "How many submissions are pending review (4)? How many were accepted today (2)? Which row is the persona's (عبدالرحمن السعد / تلاوة سورة الضحى)?" — entirely from the page content. All 6 acceptance scenarios in spec.md US5 must pass.

### Implementation for User Story 5

- [x] T026 [US5] Create `pages/teacher/homework-review.html` page-level scaffold per `contracts/homework-review.md`: same shell pattern as T003 BUT using the **teacher sidebar** hard-embedded verbatim from `pages/teacher/dashboard.html` lines 33–125 — all **11 entries** in this exact order: (1) لوحة التحكم → `dashboard.html`, (2) حصصي → `my-classes.html`, (3) الحصص المباشرة → `live-sessions.html`, (4) **مراجعة الواجبات → `homework-review.html`** (carries `is-active` + `aria-current="page"`), (5) تقارير الطلاب → `student-reports.html`, (6) الحضور → `attendance.html`, (7) الأرباح → `earnings.html`, (8) السلف → `advances.html`, (9) التقويم → `calendar.html`, (10) الرسائل → `messages.html`, (11) الملف الشخصي → `profile.html`. **All 11 `<a>` elements MUST be present** — do NOT strip the inactive entries even though their target pages do not yet exist (planned future destinations are acceptable per the constitution). Use the teacher-shell header with the teacher contextual greeting "الأستاذ أحمد العمري"; `<title>` = `مراجعة الواجبات — لوحة المعلم — منصة إدارتي`; `<main>` content uses `max-w-screen-xl mx-auto space-y-6` with 4 placeholder `<section>` skeletons keyed by id `hr-header`, `hr-summary`, `hr-filters`, `hr-queue` for tasks T027–T029. The simplest implementation strategy is to copy the entire `<aside>` block from `pages/teacher/dashboard.html`, then change only the `aria-current="page"` and `is-active` class to land on entry #4 instead of entry #1 (FR-001 / FR-002).
- [x] T027 [US5] Fill `#hr-header` (Page header) and `#hr-summary` (4 summary cards) per `contracts/homework-review.md` and `data-model.md §E9 teacher-side`: header with `<h1>مراجعة الواجبات</h1>` + subtitle `راجع تسليمات طلابك وقدّم ملاحظات تربوية واضحة.`. Then `#hr-summary` `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4` containing 4 cards in this order: (1) بانتظار المراجعة — count 4 — info tint with `inbox` icon; (2) تم القبول اليوم — 2 — success tint with `check-circle` icon; (3) تحتاج تعديل — 1 — warning tint with `alert-triangle` icon; (4) تسليمات متأخرة — 1 — danger tint with `alert-octagon` icon. Each card uses the same pattern as the student-side summary cards (T004): `bg-{tint}-50 border border-{tint}-200 rounded-2xl p-4 md:p-5 flex items-start gap-3`, leading rounded-icon block, large count, sm-text label.
- [x] T028 [US5] Fill `#hr-filters` (Search + 4 filter dropdowns) per `contracts/homework-review.md` and `data-model.md §E10 teacher-side`: outer card `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200 space-y-4`. Search row with `<input type="text" name="hr-search" placeholder="ابحث باسم الطالب أو الواجب..." class="…">` and leading search icon. Filter row `grid grid-cols-2 lg:grid-cols-4 gap-3` containing 4 labeled `<select>` controls per E10 teacher-side: الدورة (5 options including All / Quran L3 / Arabic / Islamic Studies / Math), الطالب (9 options including All + 8 student names from §E11), الحالة (8 options — the 5 teacher-side options + متأخر + بانتظار المراجعة + الكل), التاريخ (5 options: الكل / اليوم / آخر 24 ساعة / آخر 7 أيام / مخصص). All visual-only.
- [x] T029 [US5] Fill `#hr-queue` (Queue table desktop + mobile cards — 8 rows) per `contracts/homework-review.md` and `research.md §R9`: section header `<h2 class="text-base font-semibold mb-3">قائمة التسليمات (8)</h2>`. Render `<table class="hidden md:table w-full text-sm border-collapse">` with thead carrying 7 columns (الطالب / الواجب / الدورة / وقت التسليم / الحالة / المحاولة / إجراء) and tbody containing exactly 8 `<tr>` rows from R9 inventory. **Row 1 (featured — highlight with `bg-info-50/30`)**: avatar ع.س + عبدالرحمن السعد / المستوى الثالث | تلاوة سورة الضحى مع تطبيق أحكام التجويد | القرآن المستوى الثالث | اليوم 09:14 ص (caption `قبل ساعتين`) | بانتظار المراجعة | المحاولة 1 من 3 | "مراجعة" → `homework-submission-details.html`. **Rows 2-8** per R9: محمد السعيدي / كتابة فقرة "أهمية القراءة" / Arabic / اليوم 08:42 / بانتظار / 1 من 3; عمر القحطاني / تلاوة سورة الفلق / Quran L3 / أمس 21:18 / بانتظار / 2 من 3 (إعادة تسليم); أحمد الغامدي / حلّ أسئلة الفصل الأول / السيرة / أمس 18:05 / بانتظار / 1 من 3; فيصل العتيبي / حفظ سورة الإسراء / Quran L2 / أمس 14:30 / مقبول (تم التقييم اليوم) / 1 من 3; ريم الدوسري / تأمل في معاني سورة القدر / التفسير / اليوم 11:02 / مقبول (تم التقييم اليوم) / 1 من 3; ليلى المنصور / حلّ تمارين النحو / النحو / منذ 3 أيام / يحتاج تعديل / 1 من 3; سارة الحربي / تسميع جدول الضرب / الرياضيات / منذ 5 أيام / متأخر / 0 من 3. Each row's "مراجعة" CTA links to `homework-submission-details.html`. Then render mobile mirror `<div class="md:hidden space-y-3">` with 8 stacked `<article>` cards containing the same 8 rows. Append static pagination caption `<p class="text-xs text-text-muted text-center mt-4">صفحة 1 من 1 — 8 من 8 تسليمات</p>`.

**Checkpoint**: User Story 5 complete. The teacher homework-review queue renders all 8 rows + 4 summary cards + 4 filter dropdowns + search; SC-005 is validated by inspection. Cross-page anchor confirmed: row 1 reconciles with the persona's row 2 on `assignments.html` (same student, same assignment, same submitted-at).

---

## Phase 8: User Story 6 — Teacher Submission Grading (Priority: P1)

**Goal**: Build the teacher's grading workspace — two-column layout (left context: student card + assignment card + submitted files + student note quote; right form: overall evaluation + status selector + Quran evaluation form + actions). The form is pre-filled to demonstrate the populated state; the R5 helper caption documents the teacher→student status mapping; the R4 independence caption mirrors the student-side rubric.

**Independent Test**: Open `pages/teacher/homework-submission-details.html` from the file explorer. A viewer must find — without further interaction — every required form element: numeric grade input (65), feedback textarea, strengths/improvements lists, 5-option status selector with يحتاج تعديل selected, request-resubmission checkbox checked, 4-axis Quran evaluation form (85/60/75/70), mistakes count input (5), ayahs tag input (3 tags), recitation notes textarea, and Save button. All 6 acceptance scenarios in spec.md US6 must pass.

### Implementation for User Story 6

- [x] T030 [US6] Create `pages/teacher/homework-submission-details.html` page-level scaffold per `contracts/homework-submission-details.md`: same teacher-shell pattern as T026 (all 11 teacher-sidebar entries hard-embedded verbatim from `pages/teacher/dashboard.html` — لوحة التحكم / حصصي / الحصص المباشرة / **مراجعة الواجبات (active)** / تقارير الطلاب / الحضور / الأرباح / السلف / التقويم / الرسائل / الملف الشخصي) with `is-active` + `aria-current="page"` on the مراجعة الواجبات entry; `<title>` = `مراجعة تسليم: تلاوة سورة الضحى — لوحة المعلم — منصة إدارتي`; `<main>` content uses `max-w-screen-xl mx-auto space-y-6`. Inside: place `#hsd-header` (breadcrumb + h1 + queue prev/next nav) and `#hsd-body` (two-column grid wrapper) for tasks T031–T035. The `#hsd-body` wrapper is `<section id="hsd-body" class="grid grid-cols-1 lg:grid-cols-12 gap-6">` containing two child `<div>`s: left column `<div class="lg:col-span-5 space-y-5">` (will hold T031–T032 articles) and right column `<div class="lg:col-span-7 space-y-5">` (will hold T033–T035 articles).
- [x] T031 [US6] Fill `#hsd-header` and the **left column** student/assignment cards per `contracts/homework-submission-details.md` and FR-060 / FR-061: header row `flex items-center justify-between flex-wrap gap-3` containing breadcrumb (`<a href="homework-review.html">مراجعة الواجبات</a> / تلاوة سورة الضحى مع تطبيق أحكام التجويد`) + `<h1>مراجعة تسليم: تلاوة سورة الضحى مع تطبيق أحكام التجويد</h1>`, and inline-end queue nav with two `<a>` links: `← السابق` (→ `homework-review.html#prev-row`) and `التالي: ليلى المنصور — حلّ تمارين النحو →` (→ `homework-review.html#next-row`). Then in left column: (a) **`#hsd-student-card`** `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` with header (avatar large `ع.س` in `bg-primary-700 text-white` + name `عبدالرحمن السعد` + caption `المستوى الثالث · القرآن الكريم`) + `<dl class="grid grid-cols-2 gap-3 text-sm">` with 4 fields per E11/R14: ولي الأمر = عبدالله السعد; الواجبات المنجزة = 18 واجباً (success tint); نسبة الالتزام = 85%; آخر تقييم = 65 / 100. (b) **`#hsd-assignment-card`** `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` with h2 `معلومات الواجب` + `<dl class="space-y-3 text-sm">` of 5 rows: العنوان = تلاوة سورة الضحى مع تطبيق أحكام التجويد; الدورة = القرآن الكريم — المستوى الثالث; تاريخ الإنشاء = 5 مايو 2026; تاريخ التسليم = 9 مايو 2026; المحاولة = 1 من 3.
- [x] T032 [US6] Fill the remaining left-column articles `#hsd-files` (submitted files) and `#hsd-student-note` per `contracts/homework-submission-details.md` and `research.md §R10`: files article `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` with h2 `الملفات المسلَّمة` + 2 file rows: (a) **Primary audio** `bg-violet-50 border border-violet-200 rounded-xl p-4 flex items-center gap-3` with audio play-stub button (per R10) + filename `تسجيل-سورة-الضحى-المحاولة-1.mp3` + meta `MP3 — 1.1 MB — مدة 01:18` + inline-end success pill `الملف الأساسي`; (b) **Supplementary image** `bg-emerald-50 border border-emerald-200 rounded-xl p-4 flex items-center gap-3` with `image` icon block + name `صورة-من-المصحف-سورة-الضحى.png` + meta `PNG — 320 KB — أبعاد 1080×1920` + `<a href="#planned" class="text-xs text-accent-700">عرض</a>`. Then student-note article `bg-primary-50 border-r-4 border-primary-500 rounded-2xl p-5` with header (`quote` icon + h2 `ملاحظة الطالب`) + `<blockquote class="text-sm leading-7 ps-3 border-s border-primary-200">راجعت الآيات قبل التسجيل، وحاولت تطبيق الإخفاء الشفوي قدر استطاعتي. أتمنى ملاحظاتكم على المخرج الصحيح للحرف.</blockquote>`.
- [x] T033 [US6] Fill the right-column **`#hsd-overall`** (Overall evaluation panel) per `contracts/homework-submission-details.md` and FR-062 + `research.md §R4`: article `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` containing in order: (a) h2 `التقييم العام`; (b) numeric grade input row — `<label for="hsd-grade">الدرجة (من 100)</label>` + `<input type="number" id="hsd-grade" min="0" max="100" value="65" class="w-28 …">` + a flex-1 `h-2 bg-slate-200 rounded-full` progress bar with `bg-warning-500` fill at 65%; (c) **independence caption** (R4 — mirrors student-side): `<p class="text-xs text-text-muted leading-7 mb-5 italic bg-slate-50 rounded-lg p-3">المحاور القرآنية أدناه تفصيلية لمهارات الطالب في كل محور. التقييم العام أعلاه تقدير شامل يشمل جودة التسجيل، الالتزام، وسلوكيات الأدب القرآني — لا يُشترط أن يكون مساوياً لمتوسط المحاور.</p>`; (d) feedback textarea — `<label for="hsd-feedback">ملاحظة المعلم</label>` + `<textarea id="hsd-feedback" rows="6">…4-sentence pre-filled draft about إظهار good in آية 1, إخفاء needing work at آية 3 لِمَنْ آمَنَ, ghunnah short at آية 7, recommendation to listen to المنشاوي…</textarea>`; (e) strengths list — `<label>نقاط القوّة</label>` + `<ul id="hsd-strengths" class="space-y-2">` with 4 `<li class="flex items-center gap-2"><input type="text" value="…"><button aria-label="حذف"><svg>…x…</svg></button></li>` rows pre-filled with the 4 strengths from US4/T022, plus a `+ إضافة نقطة` button below; (f) improvements list — same pattern with 3 inputs pre-filled with the 3 improvements from US4/T022, plus the add button.
- [x] T034 [US6] Fill `#hsd-status` (Status selector + resubmit checkbox) per `contracts/homework-submission-details.md` and FR-063 + `research.md §R5`: article `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200`; h2 `حالة التسليم`; `<div class="space-y-3">` with exactly 5 radio-card `<label>` elements per E7 teacher-side (each `flex items-start gap-3 p-3 rounded-xl border-2 …`): (1) value=accepted / مقبول (border-slate-200); (2) value=accepted-with-notes / مقبول مع ملاحظات (border-slate-200); (3) value=needs-revision / يحتاج تعديل **(default — `data-active="true"` + border-warning-500 bg-warning-50, `<input type="radio" name="hsd-status" value="needs-revision" checked>`)**; (4) value=rejected / مرفوض (border-slate-200); (5) value=deferred / تأجيل المراجعة (border-slate-200). Below the radio cluster add the **R5 helper caption** (FR-063): `<p class="text-xs text-text-muted leading-7 mt-4 bg-slate-50 rounded-lg p-3"><strong>ملاحظة:</strong> "مقبول مع ملاحظات" تظهر للطالب كـ"مقبول" مع تفاصيل في لوحة الملاحظات. "تأجيل المراجعة" تُبقي حالة التسليم لدى الطالب "قيد المراجعة" حتى تتخذ قراراً نهائياً.</p>`. Then the request-resubmission row `pt-5 border-t border-slate-100` with `<label class="flex items-start gap-3"><input type="checkbox" id="hsd-request-resubmit" checked class="mt-1"><div><p class="text-sm font-medium">طلب إعادة التسليم</p><p class="text-xs text-text-muted">سيتلقى الطالب إشعاراً ومحاولة جديدة. يُفعَّل تلقائياً عند اختيار "يحتاج تعديل".</p></div></label>`.
- [x] T035 [US6] Fill `#hsd-quran` (Quran evaluation form) and `#hsd-actions` (Action area) per `contracts/homework-submission-details.md` and FR-064 + FR-065: Quran form article `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` with h2 `تقييم الأداء القرآني` + the **R4-mirrored caption** `<p class="text-xs text-text-muted leading-7 mb-5 italic">المحاور أدناه تفصيلية لمهارات الطالب — أدخل درجة لكل محور (0-100) مع تعليق قصير. هذه الدرجات لا تُحسب تلقائياً في التقييم العام أعلاه؛ التقييم العام يبقى تقديرك الشامل.</p>`. Then 4-axis grid `grid grid-cols-1 md:grid-cols-2 gap-4` with 4 cards per E5/E8 (الحفظ 85 / التجويد 60 / النطق 75 / الطلاقة 70) — each `bg-slate-50 rounded-xl p-4` with: numeric input pre-filled with the score value, h-2 bar at the score percentage, and a small comment input pre-filled per E5 axis comments (الحفظ comment "حفظ ممتاز…", etc.). Below the grid: `grid grid-cols-1 md:grid-cols-2 gap-4 mt-5 pt-5 border-t border-slate-100` with (a) `<label for="hsd-mistakes">عدد الأخطاء المرصودة</label> <input type="number" id="hsd-mistakes" value="5">`; (b) `<label for="hsd-ayahs">آيات تحتاج إلى مراجعة</label>` + a `<div class="flex flex-wrap items-center gap-2 p-2 rounded-xl border border-slate-300">` containing 3 tag chips (`<span class="…tag chip…">الآية 3 <button>×</button></span>` for ayahs 3 / 4 / 7) + a trailing `<input type="text" placeholder="أضف رقم آية..." class="flex-1 min-w-0 border-0">`. Then `<label for="hsd-quran-notes">ملاحظات إضافية على التلاوة</label> <textarea id="hsd-quran-notes" rows="3">أنصحك بالاستماع إلى تسجيل الشيخ المنشاوي قبل المحاولة الثانية، والتركيز على التطبيق العملي للأحكام أثناء التلاوة.</textarea>`. Then **`#hsd-actions`** article `bg-white rounded-2xl p-5 md:p-6 shadow-sm border border-slate-200` containing the same 2-row pattern as T019 (action row with primary `<button type="button">حفظ المراجعة</button>` + secondary `حفظ كمسودة` + tertiary `إلغاء` → `homework-review.html`; prototype notice `هذا نموذج تجريبي — لا يتم حفظ المراجعة فعلياً.`).

**Checkpoint**: User Story 6 complete. The teacher submission-details page renders the full two-column layout (left context + right form) with both R4 and R5 captions, all 5 status options visible, all 4 Quran axes pre-filled, and the action footer; SC-006 is validated by inspection.

---

## Phase 9: Polish (Cross-Cutting Validation)

**Purpose**: Final sweeps across all six pages to confirm every Success Criterion (SC-001…SC-015) passes before sign-off. These tasks read different files / concerns and are largely parallelizable.

- [x] T036 Run `npm run build:css` from repo root and verify `assets/css/output.css` size remains ≤ 80 KB via `wc -c assets/css/output.css`. The Spec 003 baseline was ~49 KB; this feature should add ≤ 6 KB (target ≤ 55 KB). If oversized, audit the `tailwind.config.js` `content` glob and prune any unused utilities. Confirm the file rebuilds successfully (no PostCSS errors).
- [x] T037 [P] Verify `lang="ar" dir="rtl"` is set on `<html>` for each of the 6 new pages. Use: `grep -L 'lang="ar" dir="rtl"' pages/student/{assignments,assignment-details,submit-assignment,submission-feedback}.html pages/teacher/{homework-review,homework-submission-details}.html` — must list zero files (Constitution Principle III + FR-001 carry-over).
- [x] T038 [P] Verify zero `href="#"` placeholder links across all 6 pages (FR-076 / SC-007). Use: `grep -nE ' href="#"' pages/student/{assignments,assignment-details,submit-assignment,submission-feedback}.html pages/teacher/{homework-review,homework-submission-details}.html` — must return zero matches. Same-page anchors like `href="#sa-summary"` ARE allowed because the target id exists; the grep is for literal `href="#"` only. Same-page documentation anchors `href="#planned"` are also tolerated (used to mark "planned future page" attachments).
- [x] T039 [P] Verify zero Eastern Arabic-Indic digits in all 6 page files (FR-073 / SC-008). Run `grep -nP '[٠-٩۰-۹]' pages/student/{assignments,assignment-details,submit-assignment,submission-feedback}.html pages/teacher/{homework-review,homework-submission-details}.html` — must return zero matches. All numeric content (grades, dates, ages, attempts, scores, ayah numbers) MUST be in Latin digits inside Arabic copy.
- [x] T040 [P] Verify zero placeholder filler across all 6 pages (FR-078 / SC-010). Use: `grep -nEi 'lorem|Item [0-9]|TBD|FIXME|Course [0-9]|Student [0-9]' pages/student/{assignments,assignment-details,submit-assignment,submission-feedback}.html pages/teacher/{homework-review,homework-submission-details}.html` — must return zero matches.
- [x] T041 [P] Verify no JS-rendered content was introduced and `main.js` line count is unchanged (FR-077 / SC-009). Use: `grep -nE 'innerHTML|createElement|outerHTML|insertAdjacentHTML|document\.write' assets/js/main.js` — must return zero matches. Also confirm `wc -l assets/js/main.js` shows ≤ 80 LOC (no new handlers added beyond the four sanctioned ones from Spec 001).
- [x] T042 [P] Verify the **Q1 / R4 independence captions** are present on both rubric pages (FR-044 + FR-064): `grep -n 'تقدير شامل' pages/student/submission-feedback.html pages/teacher/homework-submission-details.html` — must return ≥ 1 match per file (the caption must contain the phrase "تقدير شامل" so reviewers know the diagnostic-vs-holistic distinction is made explicit). Both files must also reference "المحاور" in the caption context.
- [x] T043 [P] Verify the **Q2 / R5 helper caption** is present on the teacher submission-details page (FR-063): `grep -n '"مقبول مع ملاحظات"' pages/teacher/homework-submission-details.html` — must return ≥ 1 match. The caption must explain both "مقبول مع ملاحظات" → "مقبول" and "تأجيل المراجعة" → "قيد المراجعة" mappings.
- [x] T044 [P] Verify the **Q3 / R6 distinction** is visible on `assignments.html` (FR-014): the page must contain at least one row with status pill `جديد` AND at least one row with status pill `مطلوب التسليم`, and the two pills must use visually distinct tints (accent for جديد vs slate for مطلوب التسليم). Run `grep -E 'جديد|مطلوب التسليم' pages/student/assignments.html` and inspect the surrounding markup to confirm distinct tints are applied. The "واجبات مطلوبة (3)" summary card must reflect 1 جديد + 2 مطلوب التسليم rows.
- [x] T045 [P] Verify the **cross-page persona anchor**: the persona's "تلاوة سورة الضحى" submission appears identically on `assignments.html` (status قيد المراجعة, submitted today) AND on `homework-review.html` (status بانتظار المراجعة, submitted today, row 1 highlighted) AND on `homework-submission-details.html` (h1 + assignment card). Verify by `grep -n 'تلاوة سورة الضحى' pages/student/assignments.html pages/teacher/homework-review.html pages/teacher/homework-submission-details.html` — must return ≥ 1 match per file. The "today 09:14 ص" timestamp must be consistent across all three pages.
- [x] T046 [P] Verify the **cross-page resubmission narrative**: the persona's `أحكام الميم الساكنة` resubmission case appears with status `يحتاج تعديل` on `assignments.html` (row 1) AND as the featured case on `assignment-details.html` (h1) AND on `submission-feedback.html` (h1). Verify by `grep -n 'أحكام الميم الساكنة' pages/student/assignments.html pages/student/assignment-details.html pages/student/submission-feedback.html` — must return ≥ 1 match per file. Grade `65 / 100` must be visible on all three pages.
- [x] T047 [P] Verify the **external-link attachment** points to a real URL (FR-024 / R11): `grep -n 'https://quran.com/2' pages/student/assignment-details.html` — must return ≥ 1 match. Confirm `target="_blank"` and `rel="noopener noreferrer"` attributes are present on the same anchor.
- [x] T048 [P] Count distinct status badge usages across the 6 pages (SC-011). Verify ≥ 25 individual badge instances drawn from the Spec 001 19-badge catalog plus Spec 002/003 type chips. Expected breakdown: assignments.html (5 summary cards + 9 status filter chips + 9 row pills + 2 grade pills ≈ 25); assignment-details.html (1 status meta-cell pill + 1 attempt pill); submit-assignment.html (1 attempt pill + 1 success pill on audio block + 1 prototype-notice warning pill ≈ 3); submission-feedback.html (1 status meta pill + 1 attempt pill + 1 timeline status pill ≈ 3); homework-review.html (4 summary cards + 8 row status pills + 8 attempt sub-pills ≈ 20); homework-submission-details.html (5 radio-card labels + 1 file-state success pill ≈ 6). Total ≈ 57 individual usages; 25 is comfortably exceeded. No invented variants.
- [x] T049 [P] Cross-page audit for FR-075 (color is never the sole indicator): walk all 6 pages and verify every status pill, every Quran-axis bar, every strengths-vs-improvements panel, every method-block muted state, every payment/file-state pill pairs color with text + icon. Specifically verify: جديد vs مطلوب التسليم pills are distinguishable not just by tint but by icon (`bell` vs `clock`) and by text; success/warning/danger axis bars are accompanied by axis labels; muted method blocks carry the "غير مطلوب لهذا الواجب" caption; the prototype notices on submit/grading pages pair the warning tint with `info` icon and explicit text.
- [x] T050 [P] Responsive audit at 375 / 768 / 1024 / 1280 px on each of the 6 pages (SC-012): verify (a) no horizontal page-level scrollbar at any breakpoint on any page, (b) the sidebar drawer (mobile burger → drawer with backdrop) behaves identically to the Spec 002/003 baseline, (c) the assignments-index list and homework-review queue collapse from `<table>` to stacked `<article>` cards at < 768 px (the `hidden md:table` / `md:hidden` toggle works), (d) the homework-submission-details two-column layout collapses to a single column at < 1024 px with **context first (left column content) then form (right column content)** per R12, (e) the assignment-details meta-strip wraps from 6 cols (lg) to 3 cols (md) to 2 cols (sm), (f) the submit-assignment 7 method blocks stack vertically at all widths.
- [x] T051 [P] Cross-page navigation chain audit — student side (SC-013): from `pages/student/dashboard.html` click "عرض كل الواجبات" → land on `assignments.html`; click "عرض التفاصيل" on row 1 (يحتاج تعديل) → land on `assignment-details.html`; click "إعادة التسليم" sticky CTA → land on `submit-assignment.html`; click "إلغاء" → return to `assignment-details.html`; click "عرض ملاحظات المعلم" in the submission history → land on `submission-feedback.html`; click "إعادة التسليم الآن" → land on `submit-assignment.html`. Verify zero broken links and zero 404s along this complete chain.
- [x] T052 [P] Cross-page navigation chain audit — teacher side (SC-013): from `pages/teacher/dashboard.html` click "فتح قائمة التصحيح" → land on `homework-review.html`; click "مراجعة" on row 1 (the persona's submission) → land on `homework-submission-details.html`; click "إلغاء" → return to `homework-review.html`. Verify zero broken links and zero 404s along this complete chain.
- [x] T053 [P] Spec 002 / Spec 003 inbound wiring audit (SC-014): verify zero shell drift on prior-spec pages — no edits to `pages/student/{dashboard,learning-journey,weekly-plan,notifications,browse-courses,my-courses,course-details,live-sessions,live-session-details,session-checkout-preview}.html`, no edits to `pages/teacher/dashboard.html`, no edits to `assets/js/main.js`, no edits to `tailwind.config.js`, no edits to `assets/css/input.css`. Run `git status` from repo root and confirm only the six new HTML files + `assets/css/output.css` are listed as added/modified. Then cross-check that the dashboard's "آخر ملاحظة من المعلم" preview is consistent with — or explicitly differs from — the "ملاحظة المعلم" quote on `submission-feedback.html` (document an inline HTML comment if they differ — e.g., "Dashboard preview shows the prior week's note; this page shows today's note.").
- [x] T054 [P] Form-input label audit (FR-079) on the two interactive surfaces — `pages/student/submit-assignment.html` and `pages/teacher/homework-submission-details.html`. Verify every visible `<input>` (excluding `type="hidden"` if any), `<textarea>`, and `<select>` has a paired visible `<label for="…">` whose `for` attribute matches the input's `id`. Procedure: extract every `id="…"` value from the two files where the surrounding tag is `<input`/`<textarea`/`<select`, then assert each id has at least one matching `for="…"` value in the same file. One concrete check: `for f in pages/student/submit-assignment.html pages/teacher/homework-submission-details.html; do echo "=== $f ==="; grep -oE '(input|textarea|select)[^>]*\bid="[^"]+"' "$f" | grep -oE 'id="[^"]+"' | sort -u > /tmp/ids.txt; grep -oE 'for="[^"]+"' "$f" | sort -u > /tmp/fors.txt; comm -23 <(sed 's/id=/for=/' /tmp/ids.txt) /tmp/fors.txt; done` — must report no orphan ids. Also visually confirm: every form input has visible label text (icons MAY supplement but MUST NOT replace text labels per FR-079). Special cases: tag-input pseudo-fields and radio-card labels containing the input inside `<label>` ARE valid alternatives to explicit `for`/`id` pairing — check the markup pattern, not just the grep.
- [x] T055 Walk through the 15-row validation table in `quickstart.md §3` end-to-end and record outcomes. The feature is Done only when all 15 success criteria pass. Confirm the constitution v1.0.1 Definition of Done gates 1–12 also pass per `quickstart.md §4`.

**Final checkpoint**: Feature 004 is Done when T055 is complete with all 15 SCs and all 12 Definition-of-Done gates green.

---

## Dependencies

- **Phase 1 (Setup)**: T001 alone — read-only baseline check.
- **Phase 2 (Foundational)**: T002 alone — documentation-only sanity check; must complete before any user-story phase begins.
- **Phase 3 (US1)**: T003 → T004 → T005 → T006 → T007. Sequential, same file (`pages/student/assignments.html`).
- **Phase 4 (US2)**: T008 → T009 → T010 → T011 → T012 → T013 → T014. Sequential, same file (`pages/student/assignment-details.html`).
- **Phase 5 (US3)**: T015 → T016 → T017 → T018 → T019. Sequential, same file (`pages/student/submit-assignment.html`).
- **Phase 6 (US4)**: T020 → T021 → T022 → T023 → T024 → T025. Sequential, same file (`pages/student/submission-feedback.html`).
- **Phase 7 (US5)**: T026 → T027 → T028 → T029. Sequential, same file (`pages/teacher/homework-review.html`).
- **Phase 8 (US6)**: T030 → T031 → T032 → T033 → T034 → T035. Sequential, same file (`pages/teacher/homework-submission-details.html`).
- **Phase 9 (Polish)**: T036 must run first (CSS build); T037–T054 are all `[P]` (each reads different concerns/files); T055 runs last as the sign-off gate.

User stories US1–US6 are independent of each other once Foundational completes — they touch different files. After Phase 2 completes, US1 / US2 / US3 / US4 / US5 / US6 could in principle run in parallel by separate implementers since each touches a different file. The spec recommends US1 ship first as the MVP entry surface (the most-linked-to page across the platform).

## Suggested MVP scope

**MVP**: Complete Phase 1 (T001) + Phase 2 (T002) + Phase 3 (T003–T007) only. Result: a fully-built `pages/student/assignments.html` showing the 9-row index. The MVP unblocks every existing inbound link from Specs 001/002/003 (dashboard / weekly-plan / learning-journey / my-courses / course-details / live-session-details / browse-courses / notifications / live-sessions all currently land on a 404 — after the MVP they all land on a real page). Sufficient to validate the homework taxonomy with stakeholders before committing to the full 6-page set.

## Per-story task counts

| Phase | Story | Task count |
|---|---|---|
| 1 | Setup | 1 |
| 2 | Foundational | 1 |
| 3 | US1 — Student Assignments Index | 5 |
| 4 | US2 — Assignment Details | 7 |
| 5 | US3 — Submit Assignment | 5 |
| 6 | US4 — Submission Feedback | 6 |
| 7 | US5 — Teacher Homework Review | 4 |
| 8 | US6 — Teacher Submission Grading | 6 |
| 9 | Polish | 20 |
| **Total** |  | **55** |

## Parallel execution opportunities

- **Within stories**: None — every story edits one file sequentially.
- **Across stories (after Foundational)**: All 6 user-story phases can run in parallel because each edits a distinct HTML file. The student-side pages (US1–US4) and the teacher-side pages (US5/US6) are especially independent — different role sidebars, different file directories — so a two-implementer split (student vs teacher) is natural. Spec recommends serial implementation in priority order for narrative coherence.
- **Within Polish**: T037–T054 are all `[P]` (18 audit tasks across different concerns). Can be batched by a single agent or distributed across reviewers.

## Independent test criteria

Each user story has its own self-contained validation per the "Independent Test" sentence at the start of each phase. The story is independently testable when its single page opens correctly and all its acceptance scenarios pass:

- **US1**: assignments.html renders 5 cards + 9 chips + 4 dropdowns + 9 rows.
- **US2**: assignment-details.html renders the meta-strip + Quran brief (4 sub-blocks) + 5 attached materials + 1-attempt history + sticky CTA.
- **US3**: submit-assignment.html renders the pinned summary + 7 method blocks (audio active + 6 muted) + checklist (4+1) + visual Submit.
- **US4**: submission-feedback.html renders the meta-strip + teacher quote + strengths/improvements + 4-axis Quran rubric WITH the R4 caption + resubmission panel + attempts timeline.
- **US5**: homework-review.html renders 4 summary cards + 4 dropdowns + 8 queue rows including the persona's row 1.
- **US6**: homework-submission-details.html renders the two-column layout (4 left context articles + 4 right form articles) with the R4 caption mirrored on the Quran form AND the R5 caption beneath the radio cluster.
