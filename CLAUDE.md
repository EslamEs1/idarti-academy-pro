<!-- SPECKIT START -->
Active feature: `006-reports-attendance-exams` — Reports, Attendance, and Exams Frontend.

Read these for full context on the **active** feature (project conventions, technology choices, page structure, contracts):

- Constitution: `.specify/memory/constitution.md`
- Spec: `specs/006-reports-attendance-exams/spec.md`
- Plan (technologies, structure, decisions): `specs/006-reports-attendance-exams/plan.md`
- Phase 0 research / clarification-resolved decisions: `specs/006-reports-attendance-exams/research.md`
- Phase 1 data model / sample-data anchors: `specs/006-reports-attendance-exams/data-model.md`
- Phase 1 contracts: `specs/006-reports-attendance-exams/contracts/` (reports, monthly-report, attendance, exams, exam-details, my-children, child-reports, student-reports)
- Quickstart (build/run + 18-row SC table + 12-gate DoD): `specs/006-reports-attendance-exams/quickstart.md`

The **prior** features `001-frontend-foundation`, `002-student-dashboard-journey`, `003-courses-sessions`, `004-assignments-homework`, and `005-achievements-certificates` are the foundation this feature builds on. Spec 001 locked the design tokens. Spec 002 added the dashboard / parent dashboard / weekly-plan / learning-journey and pinned the persona's family (عبد الرحمن مؤمن + sibling سارة مؤمن with "يحتاج إلى دعم" status). Spec 003 added the courses/live-sessions surfaces. Spec 004 added the assignments/homework loop. Spec 005 shipped the achievements layer (certificates, badges, monthly-exams-passed, completed-levels) and locked the cumulative-86.6% monthly-exam-average + the 5-monthly-exam catalog (Dec 2025 → Apr 2026). Spec 006 ships eight new pages — five student (`reports`, `monthly-report`, `attendance`, `exams`, `exam-details`), two parent (`my-children`, `child-reports`), one teacher (`student-reports`) — that finally fulfill every prior-spec promise about the reporting layer (parent dashboard's `my-children.html` / `child-reports.html` / `attendance.html` links, teacher dashboard's `student-reports.html` link, student dashboard / weekly-plan / learning-journey's `reports.html` + `exams.html` links). Two clarifications: Q1 binds filenames to existing prior-spec link targets (zero prior-spec edits required); Q2 makes `exams.html` an inclusive hub whose monthly tab duplicates Spec 005's `monthly-exams-passed.html` rows (operational catalog vs achievement lens — both surfaces co-exist).

Stack at a glance: static HTML5 + TailwindCSS 3.4 (CLI build, `assets/css/input.css` → `assets/css/output.css`, committed) + minimal vanilla JS (`assets/js/main.js`, 68 lines, only sidebar drawer / dropdown / modal / tabs — **zero new JS in Spec 006**; the `exams.html` tabs reuse the existing `data-tab` handler). Optional CSS extension: a 4-selector addition to Spec 005's `@media print` block (hides `#mr-actions` / `#mr-prototype-notice` / `#cr-actions` / `#cr-prototype-notice` during print). Lucide icons inline as SVG. Tajawal font via Google Fonts CDN. All pages `dir="rtl"`, Arabic-first, Latin digits inside Arabic copy. Persona family: عبد الرحمن مؤمن (ع.م) — الصف السادس — مسار القرآن الكريم Level 3, sibling سارة مؤمن (س.م) — الصف الثالث — يحتاج إلى دعم, parent ولي أمر الطالب عبد الرحمن, primary teacher الأستاذ أحمد بن عبد الله (Quran), secondary teacher الأستاذة منى سعد (Arabic — newly named in this spec), Islamic-Studies teacher الأستاذ خالد العبدلي. **Distinguishing design discipline**: download/print/share/save/WhatsApp clickable elements appear ONLY on the 3 report-canonical pages (`monthly-report.html` + `child-reports.html` + `student-reports.html`); they MUST NOT appear on `reports.html` / `attendance.html` / `exams.html` / `exam-details.html` / `my-children.html`. SC-016 grep-validates this constraint. **Distinguishing data discipline**: April 2026 canonical figures (92% attendance / 88% homework / 88-100 monthly exam / 89-100 overall / "ممتاز" pill / `RPT-2026-04-Q03-007` report ID) reconcile byte-identically across the 4 report-canonical pages (`reports.html`'s persona row + `monthly-report.html` + `child-reports.html` + `student-reports.html`). SC-009 + SC-018 grep-validate.
<!-- SPECKIT END -->
