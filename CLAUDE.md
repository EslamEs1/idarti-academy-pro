<!-- SPECKIT START -->
Active feature: `008-payments-invoices-billing` — Payments, Invoices, Tax Settings & Family Balance Frontend.

Read these for full context on the **active** feature (project conventions, technology choices, page structure, contracts):

- Constitution: `.specify/memory/constitution.md`
- Spec: `specs/008-payments-invoices-billing/spec.md`
- Plan (technologies, structure, decisions): `specs/008-payments-invoices-billing/plan.md`
- Phase 0 research / clarification-resolved decisions: `specs/008-payments-invoices-billing/research.md`
- Phase 1 data model / sample-data anchors: `specs/008-payments-invoices-billing/data-model.md`
- Phase 1 contracts: `specs/008-payments-invoices-billing/contracts/` (student-payment-history, student-invoice-details, parent-invoices, parent-family-balance, admin-payments, admin-invoices, admin-invoice-details, admin-tax-settings, admin-family-balances, admin-create-manual-payment)
- Quickstart (build/run + 15-row SC table + 12-gate DoD): `specs/008-payments-invoices-billing/quickstart.md`

The **prior** features 001-007 are the foundation this feature builds on. Spec 001 locked the design tokens. Spec 002 added the dashboard / parent dashboard / weekly-plan / learning-journey and pinned the persona's family (عبد الرحمن مؤمن + sibling سارة مؤمن with "يحتاج إلى دعم" status). Spec 003 added the courses/live-sessions surfaces. Spec 004 added the assignments/homework loop. Spec 005 shipped the achievements layer (certificates, badges, monthly-exams-passed, completed-levels) and locked the cumulative-86.6% monthly-exam-average + the 5-monthly-exam catalog (Dec 2025 → Apr 2026). Spec 006 shipped the reporting layer (8 pages: student reports / monthly-report / attendance / exams / exam-details + parent my-children / child-reports + teacher student-reports) and locked the April 2026 canonical figures (92% attendance / 88% homework / 88-100 monthly exam / 89-100 overall / "ممتاز" / `RPT-2026-04-Q03-007`). Spec 007 shipped seven new pages (social-hub / post-details / leaderboard / calendar — student + admin + teacher) and locked the persona's leaderboard rank #6 (general) / #2 (most-improved) plus the 5-reaction-quintet privacy posture. Spec 008 ships **ten new pages** — two student (`payment-history`, `invoice-details`), two parent (`invoices`, `family-balance`), six admin (`payments`, `invoices`, `invoice-details`, `tax-settings`, `family-balances`, `create-manual-payment`) — closing every constitutional finance sidebar entry across student / parent / admin roles. **Zero prior-spec edits** required: every existing sidebar in Specs 001-007 already points to the exact file paths required (verified by repository grep before authoring). **Four clarifications** resolved during /speckit-clarify on 2026-05-10: Q1 = family balance auto-applies on the invoice due date (INV-2026-0184 = "مدفوعة" today via 200 Mada + 170 bank + 67 auto-balance = 437; the canonical "مدفوعة جزئياً" example is INV-SES-2026-0205, 60/115 paid, due 2026-05-20 still in window); Q2 = persona himself carries the overdue INV-SES-2026-0098 (Feb private session, 92.00 ريال, overdue 74 days) AND Sister Sara carries her overdue INV-2026-0188 (April Arabic+Islamic Studies, 322.00 ريال) on parent surface; Q3 = student-type filter on admin/invoices.html uses **"ناطق بالعربية / غير ناطق بالعربية"** labels (language criterion, not residency) — replaces the original "محلي / مغترب" draft; Q4 = the "إنشاء حركة رصيد" header CTA on family-balances.html routes to `create-manual-payment.html` in balance-deposit mode (sentinel option in invoice select + "إيداع رصيد" method option), eliminating the only `href="#"` exception so SC-015 mandates zero `href="#"` across all 10 pages.

Stack at a glance: static HTML5 + TailwindCSS 3.4 (CLI build, `assets/css/input.css` → `assets/css/output.css`, committed) + minimal vanilla JS (`assets/js/main.js`, 68 lines, only sidebar drawer / dropdown / modal / tabs — **zero new JS in Spec 008**; admin/invoices.html bulk-action is visually rendered without JS handlers — master checkbox is static, `aria-disabled="true"` carries the "no rows selected" CSS state; create-manual-payment.html cascading dropdowns are pre-filtered in the rendered HTML for the demo state — no JS filter logic; the Tax-enabled toggle on tax-settings.html renders in the ON state via CSS pseudo-class; receipt-upload affordance is a visual drop-zone aesthetic with no `<input type="file">` change handler). **One-time CSS rebuild required in Polish** — `npm run build:css` regenerates output.css to pick up any new Tailwind utilities (border-dashed, font-tabular-nums, etc.); expected delta ≤ 8 KB; final size ≤ 80 KB ceiling. No `input.css` edits — JIT scan picks up the new utilities automatically. Lucide icons inline as SVG. Tajawal font via Google Fonts CDN. All pages `dir="rtl"`, Arabic-first, Latin digits inside Arabic copy. Persona family: عبد الرحمن مؤمن (STD-2024-0817) — الصف السادس — مسار القرآن الكريم Level 3, sibling سارة مؤمن (STD-2024-0818) — الصف الثالث — يحتاج إلى دعم, parent ولي أمر الطالب عبد الرحمن, family ID **FAM-2023-0211**. **Admin staff** newly named here: AHMED.K (موظف الاستقبال — recorded the 2026-04-12 Mada 200 ريال payment), SARA.M (موظفة المالية — recorded the 2026-04-25 bank transfer 170 ريال), system actor **نظام تلقائي** (the auto-applied balance event 2026-04-30). **Calendar today** = 2026-05-10 (1-day forward from Spec 007's 9 مايو 2026 to absorb the 2026-05-08 admin-adjustment row + 2026-05-05 deposit row + 2026-05-10 manual-payment-form default). **Anchor invoice INV-2026-0184**: 380 subtotal + 57 VAT 15% = 437 ريال total, fully paid via 3 streams (Mada 200 + bank 170 + auto-balance 67); shared between student/invoice-details.html (download buttons) and admin/invoice-details.html (3 admin-only panels: timeline / balance-usage / admin-notes + 5-action bar). **Persona's family-balance ledger** (FAM-2023-0211): 7 rows summing to 450.00 ريال (+500 -100 +60 -10 -67 +200 -133), demonstrating all 4 transaction types (إيداع×2, استخدام للفاتورة×2, استرداد×1, تسوية×2). **Persona's payment-history**: 7 rows demonstrating all 6 statuses (Paid×2, Partially Paid, Under Review, Unpaid, Cancelled, Overdue); Total Paid summary = 934.00 ريال (excludes the 69 Under Review). **Multi-currency demonstration**: SAR primary on student/parent surfaces; EGP demonstrated on 3 admin surfaces (admin/payments.html row 8 PAY-2026-EG-0042 = 1,710 EGP / admin/family-balances.html rows 5+6 عائلة حسن 380 + Hosni 1,540 / admin/tax-settings.html Egypt example card 1500→1710 EGP); admin/family-balances.html separates "إجمالي الرصيد بالريال" and "إجمالي الرصيد بالجنيه" tiles (currencies NEVER summed across rows). **Distinguishing zero-prior-spec-edits discipline**: every existing sidebar already points to the new file paths; the implementation only writes new files; a final `git status` audit shows ONLY 11 expected staged files (10 new HTML + rebuilt output.css). **Distinguishing zero-href-hash discipline**: Q4 eliminated the only modal-stub exception; SC-015 mandates `grep -nE 'href="#"'` returns zero matches across all 10 new pages. **Distinguishing two-views-one-entity discipline**: INV-2026-0184 renders byte-identical financial figures on student/invoice-details.html and admin/invoice-details.html — student lens has download buttons, admin lens has 3 audit-trail panels + 5-action bar; verified by side-by-side review (SC-003). **Distinguishing auto-application visibility discipline**: the single 2026-04-30 auto-balance event renders on three independent surfaces (admin timeline / parent ledger row 5 / student invoice-stream 3) all carrying the "تلقائي" pill — the academy's right-to-financial-transparency rule made operational. **Distinguishing student-type-naming discipline**: Q3 normalized the Constitution-VI Arabic-vs-Foreign distinction to "ناطق بالعربية / غير ناطق بالعربية" (language criterion); ≥ 2 "غير ناطق بالعربية" rows on admin/invoices.html (anchored to STD-2024-1311 عبدالله رحمن Indonesian + STD-2024-1402 يوسف عبد الكريم American) demonstrate the affordance. **Distinguishing VAT-line-ubiquity discipline**: every invoice rendered on every page MUST display a separate VAT line (FR-111); SC-006 grep-validates ≥ 25 VAT contexts. **Distinguishing Spec-006-academic-figures-untouched discipline**: 92% / 88% / 88-100 / 89-100 / "ممتاز" / `RPT-2026-04-Q03-007` MUST NOT appear in any of the 10 new pages (SC-012 grep-validates).
<!-- SPECKIT END -->
