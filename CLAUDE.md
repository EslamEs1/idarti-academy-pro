<!-- SPECKIT START -->
Active feature: `011-student-teacher-assignments` — Student-Teacher Assignment & Academic Supervision Frontend.

Read these for full context on the **active** feature (project conventions, technology choices, page structure, contracts):

- Constitution: `.specify/memory/constitution.md`
- Spec: `specs/011-student-teacher-assignments/spec.md`
- Plan (technologies, structure, decisions): `specs/011-student-teacher-assignments/plan.md`
- Phase 0 research / clarification-resolved decisions: `specs/011-student-teacher-assignments/research.md`
- Phase 1 data model / sample-data anchors: `specs/011-student-teacher-assignments/data-model.md`
- Phase 1 contracts: `specs/011-student-teacher-assignments/contracts/` (admin-student-teacher-assignments, admin-create-student-teacher-assignment, admin-student-teacher-assignment-details, admin-transfer-student-teacher, teacher-my-assigned-students, teacher-assigned-student-details, admin-academic-supervisors)
- Quickstart (build/run + 12-row SC table + 12-gate DoD + cross-spec greps + sidebar-diff verification): `specs/011-student-teacher-assignments/quickstart.md`

The **prior** features 001-010 are the foundation this feature builds on. Spec 001 locked design tokens. Spec 002 added dashboards and pinned the persona family (عبد الرحمن مؤمن + sibling سارة + the 3 named teachers الأستاذ أحمد / الأستاذة منى / الأستاذ خالد). Spec 003 added courses/live-sessions. Spec 004 added the assignments/homework loop. Spec 005 shipped achievements. Spec 006 shipped the reporting layer and locked **April 2026 figures** (92% attendance / 88% homework / 88-100 monthly exam / 89-100 overall / "ممتاز" / `RPT-2026-04-Q03-007`). Spec 007 shipped social-hub / leaderboard / calendar (rank #6/#2 + 5-reaction-quintet). Spec 008 shipped the **family-finance layer** (10 pages) locking anchor invoice **INV-2026-0184** (437 ريال, 3 streams + auto-balance 67) + persona family balance **FAM-2023-0211 450 ريال** + the **two-views-one-entity** dual-lens discipline + the **«ناطق بالعربية / غير ناطق بالعربية»** student-type labels. Spec 009 shipped the **teacher-payroll layer** (8 pages) locking anchor settlement **STL-2026-04-TCH-0042** (الأستاذ أحمد، 3,750 ريال صافي بانتظار الاعتماد) + multi-currency demonstration via الأستاذة هبة (EGP) + introduced staff actors **SARA.M** (موظفة المالية / manual-adjustment author) + **AHMED.K** (موظف الاستقبال / settlement reviewer) + system actor "نظام تلقائي". Spec 010 shipped **ten new admin pages** — `roles-permissions` / `role-details` / `create-role` / `users` / `user-details` / `platform-settings` / `pricing-rules` / `student-type-settings` / `notification-settings` / `audit-log` — closing the **administrative-control layer** of the platform, locking the **9 pricing rules** (8 active + 1 موقوفة) — including Rule 1 (380.00 ريال course / ناطق) + Rule 2 (600.00 ريال / غير ناطق) + Rule 6 (60.00 ريال private session / ناطق) + Rule 7 (80.00 / غير ناطق) + Rule 3 (25.00 group / ناطق) + Rule 4 (35.00 / غير ناطق) + Rule 5 (0.00 trial) + Rule 8 (1500.00 EGP) — the **14 seeded users** + 7 active roles + the dual-lens permission-matrix discipline + the cross-spec audit log citing prior-spec identifiers verbatim. Spec 011 ships **seven new pages** — 5 admin (`student-teacher-assignments` / `create-student-teacher-assignment` / `student-teacher-assignment-details` / `transfer-student-teacher` / `academic-supervisors`) + 2 teacher (`my-assigned-students` / `assigned-student-details`) — that surface the **Assignment record** as a first-class entity for the first time in the program. Every prior-spec operational page implicitly referenced an underlying student × teacher × course × level × payment-model binding; Spec 011 makes that binding visible, editable, transferable, and dual-lens-viewable. **Three clarifications** resolved during /speckit-specify on 2026-05-11: Q1 = the `academic-supervisors.html` page is a **filtered view** of the same Assignment entity rendered on `student-teacher-assignments.html` (filtered to assignment_type = «مشرف أكاديمي»); a single supervisor row appears on both pages citing the same `ASN-...` identifier — preserving the two-views-one-entity dual-lens discipline at the page-pair level. Q2 = transferred-out students appear in a separate **«طلاب سابقون — للمرجعية التاريخية»** collapsible `<details>` section on the prior teacher's `my-assigned-students.html`, AND in the «الطلاب الحاليون» primary table of the new teacher — Constitution-VI's "no silent erasure of historical state" rule operationalized at the UI level. Q3 = the 4 consequential actions (save-assignment / stop-assignment / confirm-transfer / resume-assignment) all trigger sanctioned `<div role="dialog">` modals via the existing main.js `data-modal-open` attribute pattern (lines 36-44) — same handler as Spec 010 Q3's user-details modals and Spec 009 Q1's settlement-action modals. **Zero new JS** — reuses the same data-attribute selectors already in main.js. **Sidebar edits ARE applied this spec** (the only sanctioned prior-spec touches): admin sidebar gains 2 new entries («تعيين الطلاب للمعلمين» + «المشرفون الأكاديميون») and teacher sidebar gains 1 new entry («طلابي») — identical insert across every existing `pages/admin/*.html` and `pages/teacher/*.html`, verifiable by per-file diff (SC-011).

Stack at a glance: static HTML5 + TailwindCSS 3.4 (CLI build, `assets/css/input.css` → `assets/css/output.css`, committed) + minimal vanilla JS (`assets/js/main.js`, 68 lines, only sidebar drawer / dropdown / **modal** / tabs — **zero new JS in Spec 011** (carries the Spec-010 baseline forward unchanged); **anchor assignment**: `ASN-2024-1184` (عبد الرحمن مؤمن × الأستاذ أحمد بن عبد الله × دورة القرآن الكريم — المستوى الثالث × معلم رئيسي × خاصة × دورة كاملة × ناطق بالعربية × started 2024-09-15 × 18/24 sessions remaining × **INV-2026-0184** linked + **STL-2026-04-TCH-0042** earnings-linked) — the bidirectional dual-lens anchor across Specs 008 + 009 + 011 visible on ≥ 4 of the 7 new pages per SC-003; **anchor pre-populated transfer**: `ASN-2024-1185` (سارة مؤمن × الأستاذة منى → الأستاذ خالد العبدلي, transfer-date 2026-05-15, reason «تعارض في الجدول», scope «الجلسات المستقبلية فقط»); **anchor transferred-out demo**: `ASN-2024-0902` (حسن المنصور, الأستاذ أحمد → الأستاذة منى on 2026-03-15, reason «طلب ولي الأمر» — أحمد taught حسن Qur'an L1 group sessions before the transfer) — the «طلاب سابقون» section row on الأستاذ أحمد's teacher-view (`my-assigned-students.html` is fixed to أحمد's view per FR-050, and أحمد being the previous teacher satisfies the section's defining rule) per Q2 resolution; **multi-teacher persona row**: `ASN-2024-1191` (عبد الرحمن مؤمن × الأستاذة منى × حصص جماعية إضافية × اشتراك شهري × 25.00 ريال/جلسة) — same student as ASN-2024-1184 with a different teacher, making the multi-teacher Constitution-VI rule visible literally on the assignments table. **18-row main table** = 16 teaching rows + 2 «مشرف أكاديمي» supervision-only rows = 18 (status mix: 16 نشطة + 1 موقوفة + 1 مكتملة; the «إجمالي التعيينات — 18» summary tile carries a «16 نشطة · 1 موقوفة · 1 مكتملة» sub-line so the headline isn't read as "16 active"): 12 ناطق + 6 غير ناطق ; 10 خاصة + 8 جماعية ; 9 دورة-كاملة + 4 جلسات-محددة + 3 شهري + 2 حصة-تجريبية ; 17 SAR + 1 EGP (الأستاذة هبة × يوسف عبد الله المصري × Rule 8 = 1,500.00 EGP) ; 2 «على وشك الانتهاء» rows. The `academic-supervisors.html` page seeds its own 14 supervision rows / 4 supervisors independently (page-pair dual-lens via the shared `ASN-...` identifiers — not required to be a strict subset of the 18). **Three new seed personas introduced this spec only**: **عمر شودري** (`STD-2024-0034`, age 11, جدة, «غير ناطق بالعربية», Pakistani father — drives the create-form demo state with Spec-010 Rule 2 = 600.00 ريال); **محمد إقبال شودري** (`PRT-2024-0089`, family `FAM-2024-0089`, 0 ريال balance — distinct from `FAM-2023-0211`'s 450 ريال); **حسن المنصور** (`STD-2023-0067`, age 10, الرياض, ناطق — the «طلاب سابقون» transfer-out demo; previous teacher = الأستاذ أحمد, new teacher = الأستاذة منى). The 4 named teachers (الأستاذ أحمد / الأستاذة منى / الأستاذ خالد / الأستاذة هبة) carry over from Specs 002/009 with capacity numbers seeded (الأستاذ أحمد at 12/12 — at-capacity demo on the summary tile). **14 supervision-table rows** on `academic-supervisors.html` (a separate seed from the 18-row main table) distributed as الأستاذة منى 5 / الأستاذ أحمد 4 / الأستاذ خالد 3 / الأستاذة هبة 2 = 14; supervision-status chips in 3 styles (success-green «ممتازة» / brand-primary «جيدة» / danger-amber «تحتاج اهتماماً عاجلاً»); 3 rows cite persona-family or عمر شودري verbatim (عبد الرحمن / سارة / عمر in rows 1-3). **«إشعار حفظ السجل التاريخي» warning-amber callout** on the transfer page body verbatim: «سيتم الاحتفاظ بجميع التقارير والحضور والواجبات والسجلات المالية مرتبطةً بالمعلم الحالي — لن يُحذف أي سجل سابق. الطالب سينتقل إلى المعلم الجديد ابتداءً من تاريخ النقل المحدد، ولكن سجلاته السابقة ستظل مرتبطة بالمعلم السابق للمرجعية التاريخية.». **Distinguishing two-views-one-entity (record-level) discipline** (carried from Specs 008/010): `<!-- SHARED_ASN_HEADER_START -->` block on `student-teacher-assignment-details.html` is byte-identical to the same block on `assigned-student-details.html` — verifiable by `diff` (SC-004). **Distinguishing two-views-one-entity (page-pair-level) discipline** (Q1 resolution): supervisor rows cite the same `ASN-...` identifier on both `student-teacher-assignments.html` and `academic-supervisors.html`. **Distinguishing zero-href-hash discipline**: SC-002 mandates `grep -nE 'href="#"'` returns zero matches across all 7 new pages. **Distinguishing sanctioned-modal pattern reuse**: **4 distinct sanctioned-modal blocks** (`modal-confirm-save-assignment` / `modal-confirm-stop-assignment` / `modal-confirm-transfer` / `modal-confirm-resume-assignment`), each opening a `<div role="dialog">` with the «هذا عرض مرئي فقط — لن يتم تنفيذ الإجراء فعلياً» secondary caption. Each block may be triggered from multiple buttons (e.g., the per-row «إيقاف» icons on the assignments list all point at the one shared `modal-confirm-stop-assignment` block — the established Spec-010 shared-modal pattern), so the count of `data-modal-open` triggers exceeds 4 while the count of distinct modal blocks is 4; all triggers reuse the existing `data-modal-open` / `data-modal-close` handler — zero new JS. **Distinguishing Spec-006-figures-preserved discipline**: 92% attendance + 88% homework + «ممتاز» MUST appear verbatim on the teacher's detail view; zero contradictions with Spec 006. **Distinguishing Spec-008-figures-preserved discipline**: `INV-2026-0184` + `FAM-2023-0211` + 450 ريال cited verbatim on the admin-details page; zero contradictions with Spec 008. **Distinguishing Spec-009-anchor-preserved discipline**: `STL-2026-04-TCH-0042` cited verbatim on the admin-details teacher-earning-preview tile. **Calendar today**: 2026-05-11 (one day later than Spec 010's 2026-05-10). **One-time CSS rebuild required in Polish** — `npm run build:css` regenerates output.css to pick up any new Tailwind utilities (`min-w-[1200px]` for 13-column assignments-table horizontal scroll, `grid-cols-[repeat(8,minmax(0,1fr))]` for the 8-week attendance sparkline, `aspect-square` for supervisor-strip avatars, warning-amber callout border utility, multi-chip preferred-days control utilities); expected delta ≤ 6 KB; final size ≤ 94 KB ceiling (raised from 88 KB in Spec 010). **Sidebar partial updates** ARE applied in Spec 011 (the only sanctioned prior-spec edit): admin sidebar gains «تعيين الطلاب للمعلمين» + «المشرفون الأكاديميون» (canonical position between «الطلاب» and «المعلمون») on every existing `pages/admin/*.html`; teacher sidebar gains «طلابي» (between «جدولي» and «الأرباح») on every existing `pages/teacher/*.html` — identical insert per-directory, verifiable by per-file `git diff` (SC-011). No `input.css` edits — JIT scan picks up new utilities automatically. Lucide icons inline as SVG. Tajawal font via Google Fonts CDN. All pages `dir="rtl"`, Arabic-first, Latin digits inside Arabic copy.
<!-- SPECKIT END -->
