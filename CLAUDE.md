<!-- SPECKIT START -->
Active feature: `010-admin-permissions-settings` — Admin Permissions, Roles, Platform Settings & Pricing Rules Frontend.

Read these for full context on the **active** feature (project conventions, technology choices, page structure, contracts):

- Constitution: `.specify/memory/constitution.md`
- Spec: `specs/010-admin-permissions-settings/spec.md`
- Plan (technologies, structure, decisions): `specs/010-admin-permissions-settings/plan.md`
- Phase 0 research / clarification-resolved decisions: `specs/010-admin-permissions-settings/research.md`
- Phase 1 data model / sample-data anchors: `specs/010-admin-permissions-settings/data-model.md`
- Phase 1 contracts: `specs/010-admin-permissions-settings/contracts/` (admin-roles-permissions, admin-role-details, admin-create-role, admin-users, admin-user-details, admin-platform-settings, admin-pricing-rules, admin-student-type-settings, admin-notification-settings, admin-audit-log)
- Quickstart (build/run + 17-row SC table + 12-gate DoD + cross-spec greps): `specs/010-admin-permissions-settings/quickstart.md`

The **prior** features 001-009 are the foundation this feature builds on. Spec 001 locked design tokens. Spec 002 added dashboards and pinned the persona family (عبد الرحمن مؤمن + sibling سارة + the 3 named teachers الأستاذ أحمد / الأستاذة منى / الأستاذ خالد). Spec 003 added courses/live-sessions. Spec 004 added the assignments/homework loop. Spec 005 shipped achievements. Spec 006 shipped the reporting layer and locked **April 2026 figures** (92% attendance / 88% homework / 88-100 monthly exam / 89-100 overall / "ممتاز" / `RPT-2026-04-Q03-007`). Spec 007 shipped social-hub / leaderboard / calendar (rank #6/#2 + 5-reaction-quintet). Spec 008 shipped the **family-finance layer** (10 pages) locking anchor invoice **INV-2026-0184** (437 ريال, 3 streams + auto-balance 67) + persona family balance **FAM-2023-0211 450 ريال** + the **two-views-one-entity** dual-lens discipline + the **«ناطق بالعربية / غير ناطق بالعربية»** student-type labels. Spec 009 shipped the **teacher-payroll layer** (8 pages) locking anchor settlement **STL-2026-04-TCH-0042** (الأستاذ أحمد، 3,750 ريال صافي بانتظار الاعتماد) + multi-currency demonstration via الأستاذة هبة (EGP) + introduced staff actors **SARA.M** (موظفة المالية / manual-adjustment author) + **AHMED.K** (موظف الاستقبال / settlement reviewer) + system actor "نظام تلقائي". Spec 010 ships **ten new admin pages** — `roles-permissions` / `role-details` / `create-role` / `users` / `user-details` / `platform-settings` / `pricing-rules` / `student-type-settings` / `notification-settings` / `audit-log` — closing the **administrative-control layer** of the platform. **Zero prior-spec edits** required: every existing prior-spec admin sidebar already points to `roles-permissions.html` (18 refs) + `platform-settings.html` (34 refs) — verified by repository grep before authoring; the other 8 new pages are reachable from those entry points and from each other. **Three clarifications** resolved during /speckit-specify on 2026-05-10: Q1 = `pricing-rules.html` mirrors Spec 009's salary-rules retroactivity pattern (a permanent callout «تسري القاعدة على الفواتير الجديدة فقط — لا تُعدَّل الفواتير المستحقة سابقاً» beside the rule-form's effective-date picker + a footnote `* جميع قواعد التسعير تُطبَّق إلى الأمام من تاريخ السريان.` below the rules table); Q2 = `audit-log.html` carries a visual-only «تصدير CSV» secondary outline button next to the date-range filter, with `title="عرض مرئي فقط — لا يتم تنزيل ملف فعلي"` and no `download` attribute — surfaces the Export permission verb without violating Constitution V (no new JS) or Constitution II (no fake bundled file); Q3 = the 3 admin-action buttons on `user-details.html` («تعطيل الحساب» / «إعادة تعيين كلمة المرور» / «إنهاء جميع الجلسات») use the existing `data-modal-open` attribute pattern wired in main.js lines 36-44 since Spec 002 — same pattern as Spec 009 Q1 — opening 3 distinct `<div role="dialog" class="hidden">` modals each carrying a «هذا عرض مرئي فقط — لن يتم تنفيذ الإجراء فعلياً» secondary caption + تأكيد/إلغاء buttons. **Zero new JS** — reuses the same data-attribute selectors already in main.js. **Zero prior-spec edits** required: every existing teacher/admin sidebar in Specs 002-008 already points to `earnings.html` + `advances.html` + `teacher-finance.html` (verified by repository grep before authoring). **Four clarifications** resolved during /speckit-clarify on 2026-05-10: Q1 = the «اعتماد التسوية» / «تعليم كمدفوعة» / «إعادة فتح التسوية» buttons on admin/teacher-settlement-details.html trigger sanctioned **confirmation modals** (Constitution V #4 — reuses existing main.js modal handler, **zero new JS**) carrying projected post-action captions + "هذا عرض مرئي فقط" secondary captions; Q2 = the +200 ريال «حافز التميز» bonus on the April settlement carries a documented threshold caption «يُمنَح عند تجاوز نسبة الحضور 90% + تقييم إيجابي — 200 ريال ثابت» citing Spec-006's locked **92% attendance** figure verbatim from `RPT-2026-04-Q03-007`; Q3 = a permanent **regulatory callout** «تسري القاعدة على التسويات الجديدة فقط — لا تُعدَّل التسويات السابقة» renders beside the rule-form's effective-date picker on admin/teacher-salary-rules.html + a small footnote below the table — making the no-retroactive-recalculation rule explicit (mirrors Spec 008's FR-088 tax-policy callout); Q4 = the in-progress May 2026 row on teacher/earnings.html decomposes 9 sessions across the 4 rate buckets (5 ناطق-جماعية + 2 ناطق-خاصة + 1 غير-ناطق-جماعية + 1 غير-ناطق-خاصة = 150+120+50+100 = **420 ريال** — revised down from the original arithmetically-inconsistent ~720 estimate to preserve bucket-fidelity even on partial-month rows).

Stack at a glance: static HTML5 + TailwindCSS 3.4 (CLI build, `assets/css/input.css` → `assets/css/output.css`, committed) + minimal vanilla JS (`assets/js/main.js`, 68 lines, only sidebar drawer / dropdown / **modal** / tabs — **zero new JS in Spec 010** (carries the Spec-009 baseline forward unchanged); **anchor admin user**: سارة محمد عبدالله (USR-2024-0007 / Finance Manager — same SARA.M from Specs 008/009) with the dual-lens discipline applied to her permission matrix (the 6×7 = 42-cell mini-matrix on `user-details.html` is byte-identical to the corresponding 6 rows of the full 19×7 = 133-cell matrix on `role-details.html` — verifiable by `diff` per SC-005); **super-admin anchor**: محمد بن عبدالعزيز الإدريسي (USR-2024-0001 / المدير التنفيذي — newly named); **14 seeded users** distributed across the 7 active roles (2 Admin / 3 Finance Manager / 5 Teacher / 1 Parent / 1 Student / 1 Support / 1 Operations); 8th archived role «مساعد إداري» (موقوفة) demonstrates the inactive-role pattern — mirrors Spec 009's موقوفة salary-rule pattern; **9 pricing rules** anchor the academy-wide pricing config (8 active + 1 موقوفة): Rule 1 «دورة القرآن الكريم — المستوى الثالث» 380.00 ريال — ناطق بالعربية (matches Spec 008 INV-2026-0184 subtotal exactly) + Rule 2 same course غير ناطق 600.00 ريال (Constitution-VI differential) + Rule 3 «حصة جماعية ساعة واحدة» 25.00 ريال — ناطق + Rule 4 same 35.00 ريال — غير ناطق + Rule 5 «حصة تجريبية مجانية» 0.00 ريال (zero-price edge case) + Rule 6 «حصة خاصة» 60.00 ريال — ناطق (matches Spec 008 INV-SES-2026-0205) + Rule 7 same 80.00 ريال — غير ناطق + Rule 8 Egypt-tier course 1500.00 EGP (matches Spec 008 tax-settings example) + Rule 9 Egypt-tier session 80.00 EGP — موقوفة. **24 audit events** span 7 days × 11 modules × 6 actors with cross-spec traceability: 4 prior-spec identifiers cited verbatim — STL-2026-04-TCH-0042 (Spec 009, events 1+6) + INV-2026-0184 (Spec 008, event 2) + FAM-2023-0211 (Spec 008, events 2+20) + RPT-2026-04-Q03-007 (Spec 006, event 17) + 2 «نظام تلقائي» actor events (rows 2+15) + 2 empty-payload «—» login events (rows 3+24); all IPs masked first-and-last-octet (`94.***.***.118` / `78.***.***.45` / `212.***.***.91`). **28 notification toggles** in a 7-row × 4-channel grid (15 ON / 13 OFF — including the deprecated «تذكير دورة جديدة» row with all 4 OFF); تذكير الدفع is the only category with all 4 channels ON (payment urgency); تذكير مراجعة المعلم is admin-internal (Platform-only). **Platform settings** seven configuration cards: KSA + SAR primary + EGP secondary chip (with the «العملات الثانوية المسموح بها — لا يتم جمعها مع العملة الأساسية في أي ملخص» caption per FR-032) + Arabic primary + English secondary + RTL default + invoice-footer textarea + certificate issuer/signature/seal placeholders. **Distinguishing zero-prior-spec-edits discipline** (carried from Specs 008/009): every existing prior-spec admin sidebar already points to `roles-permissions.html` + `platform-settings.html`; Spec 010 only writes new files; final `git status` audit shows ONLY 11 expected staged files (10 new HTML + rebuilt output.css). **Distinguishing zero-href-hash discipline** (carried from Spec 008 SC-015 + Spec 009 SC-004): SC-002 mandates `grep -nE 'href="#"'` returns zero matches across all 10 new pages — sidebar / dropdown / footer captions use non-link text. **Distinguishing two-views-one-entity discipline** (carried from Specs 008/009): Finance Manager's permission matrix renders byte-identically across `role-details.html` (6 rows of full 19-row matrix wrapped in `<!-- MATRIX_ROWS_11_16_START -->` markers) and `user-details.html` (entire 6-row mini-matrix wrapped in `<!-- MINI_MATRIX_START -->` markers) — verifiable by `diff` (SC-005). **Distinguishing Spec-006-academic-figures-untouched discipline**: 88% / 88-100 / 89-100 / "ممتاز" MUST NOT appear in any of the 10 new pages (only RPT-2026-04-Q03-007 cited verbatim in audit-log event 17 as the title of the exported report). **Distinguishing Spec 008/009 financial-figures-untouched discipline**: INV-2026-0184 + FAM-2023-0211 + STL-2026-04-TCH-0042 appear only as cross-references in audit-log entries; no figures invented or contradicted. **One-time CSS rebuild required in Polish** — `npm run build:css` regenerates output.css to pick up any new Tailwind utilities (`min-w-[900px]` for matrix horizontal-scroll, `grid-cols-[repeat(7,minmax(60px,1fr))]` for matrix grid, peer-based switch utilities for the 28-toggle notification grid); expected delta ≤ 8 KB; final size ≤ 88 KB ceiling (raised from 80 KB in Spec 009). **New admin staff named here**: محمد بن عبدالعزيز الإدريسي (USR-2024-0001 / المدير التنفيذي / super-admin / academy founder) + عبدالرحمن سعد الفهد (USR-2024-0002 / Admin / deputy admin) + منى عبدالله الزهراني (USR-2024-0008 / Finance Manager / colleague of SARA.M) + بدر سعود الخالد (USR-2024-0009 / Finance Manager / colleague of SARA.M) + نورة سعد الدوسري (USR-2024-0051 / Support / موقوف status). All other 9 users are persona-mapped from Specs 002 + 008 + 009 (5 teachers + 1 parent + 1 student + SARA.M + AHMED.K). No `input.css` edits — JIT scan picks up new utilities automatically. Lucide icons inline as SVG. Tajawal font via Google Fonts CDN. All pages `dir="rtl"`, Arabic-first, Latin digits inside Arabic copy.
<!-- SPECKIT END -->
