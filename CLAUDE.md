<!-- SPECKIT START -->
Active feature: `009-teacher-finance-payroll` — Teacher Finance, Earnings, Advances & Salary Rules Frontend.

Read these for full context on the **active** feature (project conventions, technology choices, page structure, contracts):

- Constitution: `.specify/memory/constitution.md`
- Spec: `specs/009-teacher-finance-payroll/spec.md`
- Plan (technologies, structure, decisions): `specs/009-teacher-finance-payroll/plan.md`
- Phase 0 research / clarification-resolved decisions: `specs/009-teacher-finance-payroll/research.md`
- Phase 1 data model / sample-data anchors: `specs/009-teacher-finance-payroll/data-model.md`
- Phase 1 contracts: `specs/009-teacher-finance-payroll/contracts/` (teacher-earnings, teacher-advances, teacher-settlement-details, admin-teacher-finance, admin-teacher-salary-rules, admin-teacher-advances, admin-create-teacher-advance, admin-teacher-settlement-details)
- Quickstart (build/run + 16-row SC table + 12-gate DoD + cross-spec greps): `specs/009-teacher-finance-payroll/quickstart.md`

The **prior** features 001-008 are the foundation this feature builds on. Spec 001 locked the design tokens. Spec 002 added the dashboard / parent dashboard / weekly-plan / learning-journey and pinned the persona's family (عبد الرحمن مؤمن + sibling سارة مؤمن with "يحتاج إلى دعم" status) and the three named teachers (الأستاذ أحمد بن عبد الله / الأستاذة منى سعد / الأستاذ خالد العبدلي). Spec 003 added the courses/live-sessions surfaces. Spec 004 added the assignments/homework loop. Spec 005 shipped the achievements layer. Spec 006 shipped the reporting layer (8 pages) and locked the April 2026 canonical figures (92% attendance / 88% homework / 88-100 monthly exam / 89-100 overall / "ممتاز" / `RPT-2026-04-Q03-007`). Spec 007 shipped seven new pages (social-hub / leaderboard / calendar) and locked the persona's leaderboard rank #6/#2 + 5-reaction-quintet. Spec 008 shipped the **family-finance** layer (10 pages: 2 student + 2 parent + 6 admin) closing every constitutional finance sidebar entry on those three roles, locked anchor invoice **INV-2026-0184** (437 ريال, 3 streams + auto-balance 67), persona's **FAM-2023-0211 family balance 450 ريال**, established the **two-views-one-entity** dual-lens discipline, and locked the **«ناطق بالعربية / غير ناطق بالعربية»** student-type labels for the Constitution-VI Arabic-vs-Foreign pricing rule. Spec 009 ships **eight new pages** — three teacher (`earnings`, `advances`, `settlement-details`) + five admin (`teacher-finance`, `teacher-salary-rules`, `teacher-advances`, `create-teacher-advance`, `teacher-settlement-details`) — closing every constitutional teacher and admin sidebar entry that pertains to **payroll, earnings, advances, and salary rules**. **Zero prior-spec edits** required: every existing teacher/admin sidebar in Specs 002-008 already points to `earnings.html` + `advances.html` + `teacher-finance.html` (verified by repository grep before authoring). **Four clarifications** resolved during /speckit-clarify on 2026-05-10: Q1 = the «اعتماد التسوية» / «تعليم كمدفوعة» / «إعادة فتح التسوية» buttons on admin/teacher-settlement-details.html trigger sanctioned **confirmation modals** (Constitution V #4 — reuses existing main.js modal handler, **zero new JS**) carrying projected post-action captions + "هذا عرض مرئي فقط" secondary captions; Q2 = the +200 ريال «حافز التميز» bonus on the April settlement carries a documented threshold caption «يُمنَح عند تجاوز نسبة الحضور 90% + تقييم إيجابي — 200 ريال ثابت» citing Spec-006's locked **92% attendance** figure verbatim from `RPT-2026-04-Q03-007`; Q3 = a permanent **regulatory callout** «تسري القاعدة على التسويات الجديدة فقط — لا تُعدَّل التسويات السابقة» renders beside the rule-form's effective-date picker on admin/teacher-salary-rules.html + a small footnote below the table — making the no-retroactive-recalculation rule explicit (mirrors Spec 008's FR-088 tax-policy callout); Q4 = the in-progress May 2026 row on teacher/earnings.html decomposes 9 sessions across the 4 rate buckets (5 ناطق-جماعية + 2 ناطق-خاصة + 1 غير-ناطق-جماعية + 1 غير-ناطق-خاصة = 150+120+50+100 = **420 ريال** — revised down from the original arithmetically-inconsistent ~720 estimate to preserve bucket-fidelity even on partial-month rows).

Stack at a glance: static HTML5 + TailwindCSS 3.4 (CLI build, `assets/css/input.css` → `assets/css/output.css`, committed) + minimal vanilla JS (`assets/js/main.js`, 68 lines, only sidebar drawer / dropdown / **modal** / tabs — **zero new JS in Spec 009**; the Q1 confirmation modals on admin/teacher-settlement-details.html reuse the existing **`data-modal-open`** (on trigger button — value = modal element ID) + **`data-modal-close`** (on close button inside `<div role="dialog" class="hidden">`) attribute pattern wired in main.js since Spec 002 (lines 36-44), no new selectors, no new event listeners; admin/teacher-finance.html bulk-action affordance + admin/teacher-advances.html action cells are visually rendered without JS handlers; create-teacher-advance.html cascading dropdowns + dependent-field visibility are pre-rendered for the demo state — no JS state-change). **One-time CSS rebuild required in Polish** — `npm run build:css` regenerates output.css to pick up any new Tailwind utilities (`font-tabular-nums` for 4-bucket numeric alignment, `divide-y` for 11-line totals separators, `min-w-[850px]` for the 10-column salary-rules table); expected delta ≤ 6 KB; final size ≤ 80 KB ceiling. No `input.css` edits — JIT scan picks up the new utilities automatically. Lucide icons inline as SVG. Tajawal font via Google Fonts CDN. All pages `dir="rtl"`, Arabic-first, Latin digits inside Arabic copy. **Anchor teacher**: الأستاذ أحمد بن عبد الله الزهراني (TCH-2024-0042) — مدرّس القرآن الكريم — تاريخ التعيين 2024-09-01 — عقد دائم — الجنسية سعودية — الراتب SAR — مصرف الراجحي — IBAN SA03********0042 (masked). His **April 2026 settlement STL-2026-04-TCH-0042** is the dual-lens anchor: 52 sessions (40 ناطق + 12 غير-ناطق; 32 جماعية + 20 خاصة) → 4 rate buckets 720 + 960 + 400 + 400 = **2,480 ريال** session earnings; course-percentage 20% × 8,000 = **1,600 ريال**; bonus +200 (حافز التميز Q2-justified by the 92% Spec-006 figure); manual deduction -80 (تأخّر جلستين); 2 advance installments -200 (ADV-2026-0012 final 3/3) -250 (ADV-2026-0023 month 1/4) = **-450**; net **3,750 ريال** in **بانتظار الاعتماد** state today (expected approval 2026-05-12 / payment 2026-05-15). Renders byte-identically across `pages/teacher/settlement-details.html` (read-only download button + non-link footer) and `pages/admin/teacher-settlement-details.html` (3 admin-only panels: سجل-الاعتماد timeline / تعديلات-يدوية / أثر-السلف + 5-button action bar with 3 Q1 sanctioned modals + bank-info panel + comparison mini-panel). **Persona advance ledger** (الأستاذ أحمد): 3 advances totalling 2,200 ريال received / 1,450 ريال paid back / 750 ريال remaining (all on ADV-2026-0023 over May+June+July). **Settlement state ladder** (5 states, demonstrated): مسودة (implicit on May 9-session in-progress 420 ريال row) → بانتظار الاعتماد (April الأستاذ أحمد + الأستاذة فاطمة + الأستاذ يوسف) → معتمدة (April الأستاذة منى + الأستاذة هبة) → مدفوعة (March الأستاذ أحمد paid 2026-04-08 + April الأستاذ خالد paid 2026-05-08); مرفوضة side-branch demonstrated on advance ADV-2026-0028 not on a settlement. **Advance state ladder** (5 states, novel شبه-مكتملة): مكتملة (ADV-2025-0089) / شبه-مكتملة (ADV-2026-0012 — final installment in pending settlement) / نشطة (ADV-2026-0023 + 0019 + 0024 + EG-0007) / بانتظار-الاعتماد (ADV-2026-0031) / مرفوضة (ADV-2026-0028, "تجاوز سقف الراتب الشهري"). **Multi-currency demonstration**: SAR primary across all teacher-side pages + 5 of 6 admin-table rows; EGP via الأستاذة هبة عبد الفتاح (TCH-2024-0089, Cairo, عقد بالحصة, EGP) on 3 admin surfaces (teacher-finance row 6 net 3,950 EGP / teacher-advances ADV-2026-EG-0007 row 1,000 EGP / teacher-salary-rules rule 7 rates 45+105 جنيه). admin/teacher-finance.html separates "إجمالي المستحقات بالريال" and "إجمالي المستحقات بالجنيه" tiles (currencies NEVER summed). **Salary-rules table** (8 rules covering all 4 rate mechanisms): rules 1-4 demonstrate per-session + course-percentage; rule 5 demonstrates per-session-only (no %, no fixed); rule 6 الأستاذ يوسف demonstrates the **fixed monthly amount** (1,200 ريال شهرياً); rule 7 الأستاذة هبة demonstrates EGP-only foreign rates; rule 8 demonstrates the **موقوفة** state with «إعادة تفعيل» CTA. **Distinguishing zero-prior-spec-edits discipline**: every existing teacher/admin sidebar already points to `earnings.html` / `advances.html` / `teacher-finance.html`; the implementation only writes new files; final `git status` audit shows ONLY 9 expected staged files (8 new HTML + rebuilt output.css). **Distinguishing zero-href-hash discipline (carried from Spec 008 SC-015)**: SC-004 mandates `grep -nE 'href="#"'` returns zero matches across all 8 new pages — the FR-039 «تواصل مع الإدارة» footer is non-link text + the FR-086 «تعديل بيانات الحساب» on admin-side is a static disabled caption. **Distinguishing read-only-for-teacher discipline**: SC-010 mandates zero `<input type="number">` on the 3 teacher-side pages — only filter/search inputs (sanctioned per Constitution V) are permitted; financial-edit inputs are admin-side only. **Distinguishing two-views-one-entity discipline (carried from Spec 008)**: STL-2026-04-TCH-0042 renders byte-identical financial figures on teacher/settlement-details.html and admin/teacher-settlement-details.html — the same eleven financial lines verifiable by `diff` (SC-005). **Distinguishing advance-cross-reference discipline**: ADV-2026-0012 + ADV-2026-0023 each appear ≥ 6 contexts across the 8 files (SC-006 grep) — settlement → advance → installment-event traceability is fully visible. **Distinguishing Arabic-vs-Foreign-rate-distinction discipline (carried from Spec 008 Q3)**: «ناطق بالعربية» + «غير ناطق بالعربية» each appear ≥ 12 contexts across the 8 files (SC-007 grep) — uses identical labels to Spec 008. **Distinguishing Q2-bonus-rule discipline**: the «سياسة حافز التميز» caption (≥90% + 200 ثابت) appears on both settlement-details pages with the 92% Spec-006 figure cited verbatim. **Distinguishing Q3-retroactivity discipline**: the rule-form callout «تسري القاعدة على التسويات الجديدة فقط» + the table footnote `* جميع القواعد تُطبَّق إلى الأمام من تاريخ السريان.` appear on admin/teacher-salary-rules.html — mirrors Spec 008's FR-088 tax-policy regulatory-transparency pattern. **Distinguishing Spec-006-academic-figures-untouched discipline**: 88% / 88-100 / 89-100 / "ممتاز" MUST NOT appear in any of the 8 new pages (SC-012 grep) — only the 92% figure is referenced (verbatim, only inside the Q2 bonus caption). **New admin staff named here**: SARA.M (موظفة المالية — created the manual adjustments + bonus + admin notes), AHMED.K (موظف الاستقبال — reviewed the settlement on 2026-05-03), system actor "نظام تلقائي" (created the settlement on 2026-05-01) — all carried over from Spec 008. **New teachers introduced**: الأستاذة فاطمة الزهراني (TCH-2024-0073, رياضيات), الأستاذ يوسف القحطاني (TCH-2024-0078, لغة إنجليزية للمدارس, fixed-monthly-amount rule), الأستاذة هبة محمد عبد الفتاح (TCH-2024-0089, إنجليزية لغير الناطقين, Cairo, EGP).
<!-- SPECKIT END -->
